# Tarefa B — Camada de engajamento (som, confete, celebrações, pódio animado) + correções da Gamificação

> Contexto do agente: **limpo**. Cabe em uma sessão (1–2 dias). A Tarefa A (fundação visual) **já foi entregue** — leia `docs/design-system.md` (tokens e kit) e `docs/prompts/handoff-A.md` seções 2, 3 e 5 antes de começar.

## Contexto
- Repo: `C:\Users\Alisson\CascadeProjects\FACEIMOB`, branch `nova`. Leia `.claude/CLAUDE.md` e `.claude/rules/typescript.md`.
- Dependência já instalada pelo usuário (NÃO rode `npm install`): `canvas-confetti` + `@types/canvas-confetti`. É a única dependência nova permitida — pedido explícito do cliente.
- **Outros agentes podem editar este diretório em paralelo.** Você SÓ pode editar: `src/lib/sound.ts`, `src/components/SaleCelebration.tsx`, `src/components/NewLeadNotifier.tsx`, `src/components/MotivationalPopup.tsx`, `src/components/PipelineTopRanking.tsx`, `src/components/GamificationAdmin.tsx`, `src/pages/Gamification.tsx`, `src/hooks/useGameRanking.ts`, `src/integrations/supabase/game.ts`, arquivos **novos** em `src/components/engagement/` e `src/lib/engagement/`, e — **só para montar o provider e o toggle** — `src/components/layout/AppLayout.tsx`, exatamente como descrito em `docs/prompts/handoff-A.md` §5 (import de `@/components/engagement`, `<EngagementLayer>` envolvendo `AppSidebar` + `SidebarInset`, `<SoundToggle />` antes do `RoleSwitcher`, `h-8 w-8` com `aria-label`). Nada mais no `AppLayout`.
- **Sem cor hex e sem paleta literal do Tailwind** (`text-emerald-400`, `text-amber-400`…). Os tokens já existem e compilam: `primary, secondary, muted, card, border, success, warning, info, highlight, gold, silver, bronze, chart-1..5`, todos com `-foreground`. Regra do A que vale para você: **`highlight` (amarelo da marca) é token de FUNDO** — como texto sobre fundo claro dá 1,6:1; amarelo em texto é `text-warning`. Os **5 hex que sobraram no projeto estão nos seus arquivos** (`SaleCelebration.tsx` 4 cores de confete, `Gamification.tsx` 1): zere-os. `src/lib/theme-contrast.test.ts` trava contraste — não o enfraqueça.
- Leia `docs/auditoria-2026-08-21.md`, seção 3.5 (gamificação) — achados G01–G13.
- Fonte de verdade da gamificação: `game_events` (migration `supabase/migrations/20260725120900_0010_gamification.sql`), view `game_ranking`, RPC `visible_game_ranking`, `close_game_season`, `close_month_and_season` (`0021`). Leia `src/integrations/supabase/game.ts` e `Gamification.tsx` inteiros antes de mudar. Não crie migration.

## O que o cliente pediu (atas 14/07 e 23/07)
"Sons, barulhos de comemoração, confete, coisas para aumentar o engajamento"; "ranking com animações para os 3 primeiros"; "som a cada venda". Hoje existe: `playLeadAlert` (lead novo, `NewLeadNotifier.tsx:65`) e `playSaleFanfare` + 14 divs de confete (`SaleCelebration.tsx`, realtime em `game_events` com `event_code='venda'`).

## Entregas
1. **Áudio** — `src/lib/engagement/audio.ts` (evolução de `sound.ts`; mantenha `playLeadAlert`/`playSaleFanfare` exportados e funcionando): **um** `AudioContext` compartilhado, desbloqueado no primeiro gesto (`pointerdown`/`keydown`, listener único) — hoje cada toque cria um contexto novo que nasce suspenso, e numa TV sem interação nunca toca. Volume master; mudo persistido em `localStorage` (`faceimob-sound` = `on|off`). Catálogo por síntese, sem arquivos de áudio (decisão registrada): `leadNew`, `leadClaimed` (curto, positivo), `checkin` (1 nota), `rankUp` (arpejo curto), `sale` (fanfarra), `goal` (fanfarra + acorde).
2. **Confete** — `src/components/engagement/Confetti.tsx` sobre `canvas-confetti`, presets `burst` (pequeno, a partir de um elemento/ponto), `rain` (venda), `fireworks` (meta). Cores lidas dos tokens (`getComputedStyle(document.documentElement)` de `--primary`, `--highlight`, `--success`, `--gold`; são HSL sem `hsl()` — converta). Desligado em `prefers-reduced-motion` (o `AppLayout` já tem `<MotionConfig reducedMotion="user">` para o framer-motion; o canvas precisa checar `matchMedia` por conta própria).
3. **API única** — `useCelebration()` + `<EngagementLayer />` (provider, montado por você no `AppLayout` conforme handoff-A §5; ele passa a montar `SaleCelebration`, `MotivationalPopup` e `NewLeadNotifier`, que saem do `AppLayout`) + `SoundToggle` (no header, antes do `RoleSwitcher`): `celebrate(kind, payload)` com kinds `lead_claimed | checkin | rank_up | sale | goal`. Cada kind = som + visual (toast rico ou card) + confete conforme uma tabela única. Ninguém mais toca som/confete direto.
4. **Gatilhos** (só o que o banco já emite):
   - `sale`: manter o realtime em `game_events` (`SaleCelebration`). **Corrigir:** (a) deduplicar por `ref_id` (o negócio) acumulando os `profile_id` por ~500 ms → 1 card com os nomes e 1 fanfarra (hoje venda com 2–3 corretores dispara 2–3 fanfarras e o nome troca no meio); (b) resolver o nome via `listRanking(seasonId)` (escopado) em vez de `profiles` — corretor vê "Equipe" porque o RLS de `profiles` esconde o nome; quem não estiver no escopo continua "Equipe".
   - `lead_claimed`: o ponto de sucesso de `claim_lead` fica em telas que você não pode editar (`Leads.tsx`/`LeadDetailModal.tsx`). Se houver realtime em `lead_events` (`claimed`) do próprio usuário, ligue por aí dentro do `EngagementLayer`; senão exponha `celebrate` e documente no handoff a chamada de 1 linha para o agente das telas.
   - `checkin`: realtime em `checkins` INSERT do próprio usuário (tabela publicada — migration `0020`).
   - `rank_up`: em `useGameRanking`, comparar posição anterior × nova do usuário logado; subiu → `celebrate('rank_up', {from, to})`. Só para o próprio usuário.
5. **Pódio** — `src/components/engagement/Podium.tsx`, usado em `Gamification.tsx` (e em `PipelineTopRanking` se for o mesmo dado): degraus 2-1-3, avatar grande com anel (ouro/prata/bronze = tokens `gold/silver/bronze`, já existem nos dois temas; o pódio do header em `AppLayout` já os usa — mantenha coerência visual com ele), coroa animada no 1º, entrada com stagger (framer-motion já instalado), contagem animada dos pontos, brilho suave contínuo só no 1º. Tudo respeita reduced-motion.
6. **Defeitos obrigatórios** (achados da auditoria):
   - **CRÍTICO (G01):** "Fechar Gameficação" em `Gamification.tsx:238` chama `closeGameSeason(undefined, true)` → `close_game_season(p_close_month=true)` insere `month_start(current_date)` em `closed_months` **sem migrar as propostas abertas** (o que só `close_month_and_season` faz) → o trigger `deals_guard_closed_month` passa a recusar qualquer insert/update de não-admin em negócio daquele mês-base pelo resto do mês. **Corrigir com um único ponto de fechamento:** o botão passa a chamar `close_month_and_season(p_period)` (mesma RPC do Pipeline, admin-only; ver `Pipeline.tsx:294`), com `p_period = month_start(period_start)` da temporada aberta; `AlertDialog` explicando: "Encerra a temporada, congela o ranking, trava o mês-base MM/AAAA e move as propostas abertas para o mês seguinte". Tire o parâmetro `closeMonth` de `closeGameSeason` em `game.ts` (ou fixe `false`) para ninguém reintroduzir.
   - **Modelo do cliente (21/08): o ciclo do game NÃO é mês de calendário** — começa quando o admin abre e termina quando fecha (ex.: 02/07 → 05/08). A tela mostra o período real da temporada (`period_start` → `period_end` ou "em andamento") no lugar de "Agosto 2026"; o seletor de histórico lista temporadas por `id`/`label` (hoje indexa por `YYYY-MM` em `Gamification.tsx:170, 215-217, 256-258, 332`, e a temporada nova fica invisível após um fechamento no meio do mês); "fechada" deriva de `closed_at`. Uma migration separada (agente E) vai fazer o mês-base dos negócios novos seguir a temporada aberta — não dependa dela.
   - Ranking não é reativo: assinar `game_events` INSERT por realtime e invalidar o ranking (use `useQuery` + `invalidateQueries` do TanStack Query; o `QueryClient` já existe em `App.tsx`).
   - Cores fixas de tema escuro nos componentes do jogo → tokens.
   - Sem temporada aberta, `award_game_points` retorna null em silêncio (G06): quando `getCurrentSeasonId()` for null, mostrar aviso "Jogo parado — abra uma temporada" na Gamificação e não disparar celebração de ranking.
   - `NewLeadNotifier.tsx:137,196` navega para `/pipeline` (abre a aba de negócios): deve ir para `/leads` (F04). Se sobrar tempo (G13): filtrar o realtime do gestor pelos grupos dele e trocar `isFresh` (relógio local, 20 s) por `commit_timestamp`.
7. **Acessibilidade:** celebrações com `role="status" aria-live="polite"`, `pointer-events-none`, ≤ 6 s, legíveis em claro e escuro.

## Critérios de aceite
- `npm run typecheck`, `npm run lint`, `npx vitest run`, `npm run build` verdes. Teste unitário pequeno (funções puras) para a deduplicação por `ref_id` e para a detecção de `rank_up`.
- Som ligado: check-in toca 1 nota; atender lead toca `leadClaimed` + burst; venda toca fanfarra **1 vez** + chuva de confete; subir no ranking toca arpejo + toast "Você subiu para Nº X".
- Mudo persiste ao recarregar; reduced-motion desliga confete e animações contínuas.
- Para provocar eventos localmente: `node scripts/demo.mjs preparar` e `lead` (stack local — ver cabeçalho do script). Sem Supabase local, descreva o teste manual feito.

## Entrega
Não commite. Escreva `docs/prompts/handoff-B.md`: API do `useCelebration`, gatilhos ligados/pendentes (com a linha exata para o agente das telas), arquivos tocados, o que ficou de fora. Use os componentes do kit (`PageHeader`, `SectionCard`, `StatusBadge`, `EmptyState`, `LoadingState`) na tela de Gamificação em vez de recriar — é a primeira tela a adotar o kit e serve de referência para as próximas.
