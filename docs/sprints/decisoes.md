# Decisões pendentes e registradas

Atualizado em 10/08/2026. Cada item traz o que já está decidido, o que falta e
qual story fica bloqueada. Decisão tomada vira linha em "Registradas" com data.

> **Auditoria de 08/08:** uma varredura multi-agente comparou as duas atas com o
> código real e derrubou o "tudo entregue" do plano: o modal de negócio não
> persistia nada, quatro telas rodavam sobre mock, o realtime estava vazio no
> remoto, os triggers de log derrubavam UPDATE de usuário autenticado e 43
> funções estavam executáveis por `anon`. Tudo corrigido nas migrations
> 0019–0022 + retrabalho de frontend e edge functions; as decisões novas estão
> na tabela abaixo.

---

## Pendentes — precisam do Douglas

### ~~1. Revisão gerencial dos documentos antes do CCA~~ — RESOLVIDO em 10/08

**Decidido em 10/08:** o dossiê não sai direto do corretor para o CCA. O fluxo é
**corretor → conferência do gerente → CCA**. A etapa de conferência precisa
permitir aprovar ou devolver o dossiê ao corretor.

Regras confirmadas e implementadas:

1. O negócio pode nascer sem anexos. Os documentos obrigatórios travam somente
   **Enviar para conferência do gerente**.
2. Com vários gerentes vinculados, a aprovação de um deles basta; a auditoria
   registra quem decidiu.
3. A devolução exige motivo e gera notificação persistente para os corretores.

**Estado da implementação:** migration `0028`, status e filtro no Pipeline,
ações na aba Anexos, histórico e notificações. A aprovação cria a entrada do CCA
ou da construtora externa e move o negócio para Em análise na mesma transação.
Chamada direta e arraste de card não pulam a conferência. SQL e E2E cobrem
corretor, dois gerentes, devolução e aprovação.

---

### ~~2. King Host tem API de criação de e-mail?~~ — ENCERRADO em 02/08

**Decisão do cliente: usar o Brevo.** O item morre como previsto no timebox.

Consequência: o e-mail corporativo do colaborador continua sendo criado à mão
pelo admin; o sistema não cria caixa postal. O Brevo cobre o que o requisito
realmente queria (o sistema mandar e-mail), e `_shared/brevo.ts` já está pronto.

**Story J3 encerrada sem código.** Nada a implementar.

---

### ~~3. N8N: migrar para a VPS ou aposentar?~~ — DECIDIDO em 02/08

O cliente delegou a escolha, com critério de **desempenho e confiabilidade**.

**Decisão: aposentar o N8N.** Por esses dois critérios ele perde nos dois.

**Confiabilidade.** O N8N na VPS é um ponto de falha fora do Supabase: uptime
próprio, cópia própria das credenciais (fora do cofre, portanto sem rotação
pela tela) e nenhuma relação com o RLS — um workflow com credencial ampla
ignora toda a hierarquia de visibilidade que o banco garante. As edge functions
rodam no mesmo runtime gerenciado do resto e leem o cofre.

**Desempenho.** Todo salto pelo N8N adiciona latência ao caminho do lead — e o
caminho do lead tem trava de 5 minutos, onde segundos contam. O
`meta-ads-webhook` hoje recebe e chama `assign_lead` na mesma requisição.

**O que já foi absorvido** (nada disso precisa do N8N):

| Automação | Onde roda agora |
|---|---|
| Lead do Meta entra na roleta | `meta-ads-webhook` |
| Aviso de lead perdido por prazo | `notify-dispatch` (Sprint 6) |
| Broadcast de remarketing | `sdr-whatsapp-broadcast` |
| E-mail transacional | `_shared/brevo.ts` + `submission-dispatch` |
| Agendamento recorrente | pg_cron (`0013`, `0018`) |

**Passo obrigatório antes de desligar** — e é o único que não consigo fazer
daqui, porque exige o painel do N8N: listar os workflows ativos e conferir
contra a tabela acima. Se aparecer automação fora dela, ela vira story antes do
desligamento. Se não aparecer, cancelar a assinatura.

---

### ~~4. `visits` é requisito real ou tabela especulativa?~~ — RESOLVIDO em 02/08

É requisito real: é a **visita ao imóvel com o cliente** — data marcada, data
realizada, resultado (realizada / não compareceu / cancelada) e observações. É o
elo entre atendimento e proposta, e o funil já tinha a etapa "Visita Agendada".

A pergunta expôs um problema pior que a dúvida original: **existiam dois
conceitos de visita e nenhum persistia direito.** O Pipeline tinha um botão de
marcar visita que só mudava a etapa do negócio e guardava a data em estado
local — sumia no reload —, enquanto a tabela `visits`, com o modelo completo,
nunca recebia nada.

**Resolvido:** o agendamento pelo Pipeline agora grava em `visits` além de mover
o card, e o `VisitPanel` (lead e negócio) lê e fecha a visita com resultado.

---

## Registradas

| Data | Decisão | Onde ficou |
|---|---|---|
| 30/07/2026 | Lead que estoura o prazo pode voltar ao mesmo corretor, desde que passe pela fila inteira de novo | Migration `0014`, ordenação por `last_turn_at` |
| 02/08/2026 | Login sem senha: acesso só por código no e-mail (OTP) | `Login.tsx`, `provision-broker-user`, `create-user.ts` |
| 02/08/2026 | Item de menu vira código de permissão no catálogo existente, em vez de tabela nova | Migration `0015` |
| 02/08/2026 | Leitura do cofre pelas edge functions passa por wrapper em `public` restrito a `service_role` — `private` não é exposto pelo PostgREST | Migration `0016` |
| 02/08/2026 | Pré-visualização de papel é ferramenta de admin, travada no `AuthContext` | `RoleSwitcher.tsx`, `AuthContext.tsx` |
| 02/08/2026 | Pontuação do jogo sai de `game_events`; os códigos são os do banco (`aprovado`, `distrato`), não os inventados no front | `game.ts`, `Gamification.tsx` |
| 02/08/2026 | Consolidado anual é fonte de verdade; recálculo pelo pipeline vira ação explícita | `Resultados.tsx` |
| 02/08/2026 | E-mail é pelo Brevo; King Host sai de escopo | `_shared/brevo.ts` |
| 02/08/2026 | N8N será aposentado (critério: desempenho e confiabilidade), após inventário dos workflows ativos | este documento |
| 02/08/2026 | `visits` é a visita ao imóvel; o Pipeline passa a persistir nela | `Pipeline.tsx`, `VisitPanel.tsx` |
| 02/08/2026 | Som e comemoração de venda disparam por `game_events` (`event_code = 'venda'`), não por UPDATE em `deals` | `SaleCelebration.tsx`, `lib/sound.ts` |
| 02/08/2026 | Typecheck do projeto é `npm run typecheck`; `npx tsc --noEmit` na raiz não checa arquivo nenhum | `package.json`, `.claude/CLAUDE.md` |
| 02/08/2026 | Agendamento da fila de WhatsApp lê URL e chave do cofre, não de GUC | Migration `0018` |
| 05/08/2026 | Cron `faceimob-notify-dispatch` pausado (`active = false`) enquanto não há credencial do WhatsApp — a fila em `notifications` segue sendo populada e é o que se verifica em teste | `cron.job` no remoto, `roteiro-teste-completo.md` |
| 05/08/2026 | Testador passa a ser `dev.alisson.rosa@gmail.com`. Troca de e-mail exige as três fontes juntas (`auth.users`, `auth.identities.identity_data`, `profiles`) — não há trigger de sync e o dashboard não expõe edição de e-mail | `auth`/`profiles` no remoto, seeds `050`/`059` |
| 08/08/2026 | Superfície anônima volta a ser exatamente as 3 RPCs do Diário: revoke em massa + default privileges + tripwire no harness | Migration `0019`, `tests/06_anon_surface.sql` |
| 08/08/2026 | Triggers de log (`leads/deals/cca`) viram SECURITY DEFINER — update direto por authenticated derrubava no RLS de `lead_events` e o front engolia o erro | Migration `0020`, `tests/07_core_fixes.sql` |
| 08/08/2026 | Matriz de estágios vale no servidor: `deals_guard_stage` aplica `can_enter`/`can_exit` (automação com `auth.uid()` nulo passa direto) | Migration `0020` |
| 08/08/2026 | "Status 2" (34 rótulos) persiste em `deals.status_detail`; a aba CCA do modal persiste em `cca_cases.analysis` (jsonb) | Migration `0020`, `saveLegacyDeal` |
| 08/08/2026 | Check-in exige IP identificado (RPC recusa nulo) e o `broker-checkin` confia só no último salto do `x-forwarded-for` | Migration `0020`, `broker-checkin` |
| 10/08/2026 | **Revoga a linha acima na parte do header**: medição na hospedagem mostrou que o último salto do XFF é o Global Accelerator da AWS (`13.248.114.x`, rotativo) — check-in negava ou passava conforme o nó. Fonte confiável é `sb-forwarded-for`/`cf-connecting-ip` (forja testada e descartada pelo gateway); o último salto do XFF fica só como fallback do stack local. A faixa `13.248.114.149/32` cadastrada por engano virou o IP real do testador | `broker-checkin` (v4 no remoto), `allowed_ips` |
| 10/08/2026 | Kit de demo ganha `--remote` (homologação via `.env` + `SUPABASE_SERVICE_ROLE_KEY`) e `--email` para usar o corretor real; ensaio completo da roleta validado no remoto (check-in → lead → `assign_lead` escolhendo o testador) e desfeito | `scripts/demo.mjs` |
| 08/08/2026 | Lead preso em `queued` é revarrido por cron a cada minuto; conversa SDR ativa fica fora da varredura | Migrations `0020`/`0022` |
| 08/08/2026 | Fila de dossiês à construtora ganhou o gatilho que faltava (`dispatch_pending_submissions` + cron); `requeueSubmission` zera `attempts` | Migration `0020`, `developerSubmissions.ts` |
| 08/08/2026 | "Fechar Mês" do Pipeline usa `close_month_and_season()`: propostas migram, mês trava e temporada encerra numa transação | Migration `0021`, `Pipeline.tsx` |
| 08/08/2026 | SDR ponta a ponta: origem com `sdr_agent_id` abre conversa + template de boas-vindas em vez de cair na roleta; `whatsapp-inbound-webhook` roteia respostas (lead e remarketing) para o agente; qualificação por tag `[QUALIFICADO]` dispara `sdr_handoff` | `meta-ads-webhook`, `whatsapp-inbound-webhook`, `_shared/sdrAgent.ts` |
| 08/08/2026 | Webhooks da Meta validam `X-Hub-Signature-256` quando `META_APP_SECRET` estiver no cofre; sem ele, modo de transição com aviso no log. Fallback hardcoded do verify token removido | `_shared/meta.ts`, catálogo de integrações |
| 08/08/2026 | `daily-team-info`, `submit-daily-report` e `director-weekly` aposentadas (respondem 410) — eram código morto que convertia team_id em slug com service role | stubs nas três functions |
| 08/08/2026 | `mockData.ts` apagado; Pipeline, modal de negócio, Marketing, AdminDevelopers, Dashboard e Gamification só usam dados do banco | telas citadas |
| 08/08/2026 | Modal de negócio persiste tudo por `saveLegacyDeal` (deals + 2 clientes + corretores + gerentes); comentário manual do negócio via RPC `add_deal_comment` | `newSchema.ts`, `DealDetailModal.tsx` |

| 08/08/2026 | Privilégios de tabela passam a viver nas migrations (`0023`). Um banco criado só pelas migrations nascia com as 58 tabelas inacessíveis — o remoto só funcionava porque o Supabase concedeu na criação do projeto, e o harness *simulava* os grants nos stubs | Migration `0023`, `tests/06_anon_surface.sql`, `tests/00_supabase_stubs.sql` |
| 08/08/2026 | Escalada de privilégio fechada no remoto: `assign_lead`, `award_game_points`, `release_expired_leads` e `auto_checkout_expired` eram executáveis por qualquer usuário logado. O `revoke ... from public, anon` das migrations originais não remove a concessão nominal que a plataforma dá a `authenticated` | Migration `0023`, `tests/08_frontend_rpc_grants.sql` |
| 08/08/2026 | Concessão de `execute` é em bloco para `authenticated` com revogação nominal das internas — lista de permitidos não funciona porque as policies de RLS chamam helpers e a expressão roda como o usuário | Migration `0023` |
| 08/08/2026 | E-mail de acesso passa a conter o código (`{{ .Token }}`). O template padrão do Supabase manda só o link, e a tela pede seis dígitos: quem recebesse aquele e-mail não teria o que digitar | `supabase/templates/magic_link.html`, `config.toml` |
| 08/08/2026 | `seed_tester` sai de `pg_temp` e vira arquivo próprio (`045_tester_ref.sql`). O seeder do CLI manda cada arquivo como um lote só, então criar e usar o objeto no mesmo arquivo falha — era isto que quebrava `npm run db:reset` | `seeds/045_tester_ref.sql`, `seeds/050`, `config.toml` |
| 08/08/2026 | Suíte E2E autentica pelo OTP de produção, com o código vindo da Admin API. Nada de bypass, usuário falso ou volta da senha — sem JWT real o RLS não é exercitado e a suíte passaria com corretor vendo a empresa inteira | `e2e/README.md`, `e2e/support/session.ts` |

| 10/08/2026 | **IP de host único voltou a liberar o check-in.** `ip_is_allowed` comparava com `<<` (contido *estritamente*): para um `/32` isso é sempre falso, então o IP fixo da loja era cadastrado, aparecia na lista com toast verde e não liberava ninguém. Agora `<<=` | Migration `0024`, `tests/09_ip_host.sql` |
| 10/08/2026 | Tela de SDR deixou de quebrar: `<SelectItem value="">` é recusado pelo Radix (string vazia é o valor que limpa a seleção) e derrubava a aba Agentes inteira no ErrorBoundary. Sentinela `SEM_SELECAO` | `SdrModule.tsx` |
| 10/08/2026 | Um `<h1>` por página: o título da barra do topo virou `<p>`. Dois `<h1>` quebram a navegação por cabeçalho do leitor de tela; Configurações e Gestão de dados ganharam o próprio | `AppLayout.tsx`, `Settings.tsx`, `DataManagement.tsx` |
| 10/08/2026 | Modal de negócio ganhou `DialogTitle` (visualmente oculto): sem ele o Radix avisa no console e quem usa leitor de tela ouve só "diálogo" | `DealDetailModal.tsx` |
| 10/08/2026 | Trigger `profiles_guard_admin_columns` **não** foi afrouxada para o teste passar. `service_role` não é admin (não tem `auth.uid()`), então o cenário que libera bypass de IP usa o JWT do admin E2E — o caminho real de quem concede | `e2e/support/fixtures.ts` (`comoAdmin`) |
| 10/08/2026 | Asserção de placar compara tela × `game_ranking`, não tela × número fixo: o banco é compartilhado entre projects e o número absoluto testava o isolamento do ambiente, não a regra | `e2e/{admin,broker}/gamificacao.spec.ts` |
| 10/08/2026 | **Corretor 1/2 e Gerente 1/2 trocavam de lugar no reload.** `deal_participants` não guardava o slot e a leitura o reconstruía por `created_at` — que é idêntico para todas as linhas do mesmo insert. Pior que cosmético: o corretor reabria o negócio, via o nome errado no slot obrigatório e "corrigia", trocando de verdade quem responde pelo negócio | Migration `0025`, `newSchema.ts` |
| 10/08/2026 | **Corretor perdia o próprio negócio ao salvar.** `saveLegacyDeal` apagava os participantes antes de reinserir — e é justamente estar em `deal_participants` que dá o direito de editar (`can_edit_deal`). O delete passava, o insert voltava 403 e o negócio saía da vista dele, sem como desfazer. Agora grava primeiro (upsert) e remove só as sobras | `newSchema.ts` |
| 10/08/2026 | Campo de participante vazio significa "não sei", não "remova": o corretor não enxerga o nome do gerente (RLS de `profiles` devolve só ele mesmo), então salvar apagaria o gerente do negócio — e o acesso dele junto. A limpeza é por papel e só quando o formulário trouxe alguém daquele papel | `newSchema.ts` |
| 10/08/2026 | Indicador de posição na fila passa a ouvir `checkins`, não só `lead_assignments`: quem batia ponto continuava lendo "fora da fila agora" até recarregar | `QueuePosition.tsx` |
| 10/08/2026 | Filtros do Pipeline ganharam `aria-label`. O gatilho do Select passa a mostrar o valor escolhido, então sem nome próprio o campo deixa de ser identificável — para leitor de tela e para qualquer um que precise achá-lo depois de filtrar | `Pipeline.tsx` |
| 10/08/2026 | Preparo da suíte exige o runtime de edge functions de pé. Com ele fora, o check-in não confirma e a falha aparece como "não achei 'check-in confirmado'" — ambiente disfarçado de defeito | `e2e/global-setup.ts` |
| 10/08/2026 | Dois `fixme` do SDR viraram teste de verdade junto com a correção do `SelectItem`: escolher agente de handoff e agente inicial da simulação passaram a funcionar — era o orquestrador multi-agente da ata, que não existia pela tela | `e2e/sdr/agentes.spec.ts` |
| 10/08/2026 | Roteiro de demonstração da fila de leads (`npm run demo` → `demo/fila-de-leads.webm`): app real, sessão real, e cada passo confere no banco antes de seguir — demonstração que "funciona só na tela" falha em vez de virar vídeo | `e2e/demo/fila-de-leads.spec.ts` |
| 10/08/2026 | Corretor pode ver os nomes de todos os gerentes vinculados ao próprio negócio; um negócio pode ter vários corretores e vários gerentes. A abertura vale só para participantes do mesmo negócio e não amplia a visibilidade de leads | Migration `0027`, RPC `deal_participant_names`, E2E do corretor |
| 10/08/2026 | Ranking do corretor mostra todos os corretores da equipe ativa dele. A consulta é separada da autorização de leads/negócios, portanto a carteira dos colegas continua privada | Migration `0027`, RPC `visible_game_ranking`, Gamificação e pódios |
| 10/08/2026 | Dossiê passa por corretor → gerente → CCA; negócio nasce sem anexos, obrigatórios travam o envio ao gerente, um gerente aprova e devolução exige motivo/notifica os corretores | Migration `0028`, Pipeline/Anexos, testes SQL e E2E |
| 10/08/2026 | A data exibida do check-in vem do mesmo `current_date` usado pelo banco; o navegador não recalcula “hoje”, evitando a presença invisível entre 21h e 21h30 | Migration `0029`, `current_work_date`, E2E da roleta |
| 10/08/2026 | O papel CCA recebe `menu.pipeline` porque o formulário de análise fica na aba CCA do modal de negócio; as policies já limitavam a edição ao CCA/admin | Migration `0030`, E2E completo da CCA |
| 10/08/2026 | Suíte E2E roda **serial** (`workers: 1`): todos os projects apontam para o mesmo banco e vários mexem no mesmo corretor (um atribui lead e o modal "Lead atribuído a você!" cobre a tela de outro; outro fecha a temporada no meio da contagem de pontos). Paralelismo volta quando houver um banco por worker | `playwright.config.ts` |
| 10/08/2026 | Erro de console esperado passa a ser **declarado** (`test.use({ errosEsperados: [...] })`), nunca silenciado em bloco: um teste que provoca 400/403/503 de propósito precisa do erro como prova, e qualquer outro continua reprovando | `e2e/support/fixtures.ts` |
| 10/08/2026 | Diário e Checkpoint públicos registram `last_seen_at`, portanto as RPCs são `VOLATILE`; o Checkpoint consome o contrato real `director`/`team_id`/`team_name` e link protegido devolve apenas `pin_required` antes da validação | Migration `0026`, `PublicDirectorCheckpoint.tsx`, testes anônimos |

| 10/08/2026 | O SDR pode cadastrar `lead_sources` e vincular agente/template existente; editar o conteúdo do template continua restrito a admin/marketing | Migration `0031`, `SdrModule.tsx` |
| 10/08/2026 | Importação de remarketing é atômica: lista e contatos nascem na mesma RPC e qualquer telefone inválido desfaz tudo | Migration `0031`, `import_remarketing_list`, E2E SDR |
| 10/08/2026 | Métrica de campanha usa agregado `security definer` autorizado por papel; marketing conta leads já distribuídos sem ganhar SELECT dos dados pessoais | Migration `0031`, `marketing_campaign_stats`, E2E Marketing |
| 10/08/2026 | Broadcast do WhatsApp autentica e autoriza antes de ler o cofre; o frontend mostra o corpo JSON da Edge Function em vez do erro genérico do SDK | `sdr-whatsapp-broadcast`, `functionError.ts` |
| 10/08/2026 | Aporte segue a policy existente: admin e marketing escrevem; diretor e sócio apenas leem | `Marketing.tsx`, `marketing_investments_write` |
| 21/08/2026 | **Senha volta como forma de acesso; o código por e-mail (OTP) vira alternativa.** Reverte a decisão de 02/08. A proibição de senha nasceu da senha em texto puro do Bubble — no Supabase Auth o hash fica no servidor e nunca passa pelo nosso código, então o motivo original não se aplica. O Login abre em "Entrar com senha" (`signInWithPassword`) e oferece "Receber código por e-mail" (`signInWithOtp` com `shouldCreateUser: false` + `verifyOtp`), com a mesma marca de sessão nos dois caminhos. Erro de senha tem mensagem única ("E-mail ou senha inválidos") para a tela não virar verificador de quem trabalha aqui | `Login.tsx` |
| 21/08/2026 | **Escuro é o tema padrão** e o claro é o secundário — mais próximo do que o cliente já vê hoje. `:root` é escuro, `.light` sobrepõe, `localStorage "faceimob-theme"` continua valendo | `index.css`, `useTheme.ts` |
| 21/08/2026 | **No escuro a cor de marca é a clara e o `-foreground` é a tinta navy; no claro é o inverso.** Foi o único jeito de a mesma variável servir como preenchimento (`bg-primary`) e como texto (`text-primary`) mantendo 4,5:1 nos dois temas — daí o botão primário no escuro ser azul claro com texto escuro | `index.css`, `docs/design-system.md` |
| 21/08/2026 | **`--highlight` (amarelo da marca) é token de fundo, não de texto.** No claro `text-highlight` dá 1,6:1 sobre a página. Amarelo em texto usa `--warning`, o mesmo tom escurecido | `index.css`, `StatusBadge` |
| 21/08/2026 | O tema passa a ser aplicado em `main.tsx`, antes do React montar. A classe `.light` só era escrita pelo efeito do `useTheme`, e o único componente que chamava o hook era a barra lateral — Login, 404, Diário e Checkpoint público abriam sempre no escuro mesmo com o tema claro salvo | `main.tsx` |
| 21/08/2026 | Não existe asset de logo com letra escura: `logo-faceimob.png` e `logo-faceimob-white.png` são o mesmo desenho de letra branca. No tema claro a barra lateral põe a arte sobre uma placa azul da marca até a versão escura ser entregue | `AppSidebar.tsx`, pendência com a marca |
| 21/08/2026 | **O "mês" do negócio é o do ciclo do game, não o do calendário** (ata de 14/07). `deals.month_base` nascia de `month_start(current_date)`: com a temporada de julho aberta, negócio criado em 03/08 nascia em agosto e ficava fora do fechamento de julho. Agora um trigger de INSERT grava o mês do ciclo aberto (`current_season_month()`); sem temporada aberta vale o calendário, e mês-base digitado à mão não é sobrescrito. `close_game_season` deixa de travar mês (o `p_close_month` fica na assinatura e é ignorado) — ponto único de fechamento contábil é `close_month_and_season()`, que sem argumento passa a fechar o mês do ciclo | Migration `0032`, `tests/16_game_cycle.sql` |

| 26/08/2026 | **Edge function não confia mais no gateway.** O gateway do Supabase valida a assinatura do JWT e nada além disso: a chave publicável, que vai no bundle do navegador, passava por ele e chegava a functions que rodam com service_role. `sdr-agent-chat` passa a exigir usuário autenticado **com a permissão `menu.sdr`** — a mesma que decide se a tela aparece no menu, para a lista de papéis não divergir da matriz que o admin edita. `notify-dispatch` e `submission-dispatch` passam a exigir a chave de serviço (o valor que o pg_cron manda), recusando o resto com 401 | `_shared/auth.ts`, achados S01/S04 |
| 26/08/2026 | **A porta do cron aceita duas formas de chave de serviço.** O projeto usa as chaves novas (`sb_publishable_…`/`sb_secret_…`), que não são JWT, e o valor que o `net.http_post` envia vem do cofre, cadastrado à mão. Então `requireServiceRole` aceita (a) token idêntico ao segredo `supabase/service_role_key` — cofre à frente, `Deno.env` atrás, a mesma ordem da migration 0018 — ou (b) JWT com `role = 'service_role'`, formato legado. Checar só o formato legado quebraria o cron; checar só a igualdade quebraria se o admin cadastrasse a chave legada | `_shared/auth.ts`, `_shared/secrets.ts` |
| 26/08/2026 | **Link público nasce com slug sorteado e PIN obrigatório, nos dois tipos.** O slug era, na prática, a senha, e nascia como `diretor-<nome>`: quem soubesse o nome de um diretor montava a URL. Como o navegador não faz bcrypt, não dá para exigir `pin_hash` no `INSERT`; a criação virou a RPC `create_public_link` (sorteia o slug, hasheia o PIN) e o `INSERT` direto saiu do contrato — sem policy de INSERT, o RLS recusa. O **grant** continua uniforme de propósito: `06_anon_surface.sql` exige isso da 0023, e superusuário (seed, harness) e service_role seguem inserindo direto | Migration `0033`, `tests/11_public_link_hardening.sql` |
| 26/08/2026 | **Link existente não foi invalidado.** Slug legível e ausência de PIN continuam valendo — invalidar em massa derrubaria o Diário de todas as equipes numa manhã. Quem fecha é o admin: `AdminDailyTeams` marca cada link sem PIN com aviso e oferece o botão de gerar, e o PIN passa a sair de `crypto.getRandomValues` em vez de `Math.random` | `AdminDailyTeams.tsx` |
| 26/08/2026 | **PIN de 6 dígitos ganha lockout de 15 min após 5 erros**, no resolvedor comum às três RPCs anônimas. `resolve_public_link` passou a VOLATILE para conseguir gravar a tentativa. A contagem zera no acerto (gerente que erra duas vezes não acumula dívida) e a recusa continua indistinguível: link inexistente, expirado, PIN errado e travado devolvem todos NULL. Consequência aceita: quem conhece o slug consegue travar o link por 15 min de propósito — o gerente fica sem lançar o diário nesse intervalo, e o remédio é o admin renovar o PIN, que agora limpa a trava junto | Migration `0033` |
| 26/08/2026 | **Chamada sem PIN é sondagem, não chute — e não conta para o lockout.** `DailyReport.tsx` chama `public_daily_team(slug, null)` ao montar e de novo **a cada tecla** digitada no campo do PIN (o efeito depende de `loadMonth`, que depende de `pin`): é a única forma de a tela saber se o link pede PIN, porque `has_pin` só volta no sucesso. Contar essas chamadas travava o link no quinto caractere e depois recusava até o PIN correto — o Diário ficaria inacessível para toda equipe com PIN. Só chute com PIN preenchido conta | Migration `0033`, `tests/11` bloco 4b |
| 26/08/2026 | **`create_public_link` é idempotente por (tipo, dono).** `public_links` não tem unicidade por equipe/diretor; dois cliques no botão deixariam duas URLs válidas e a tela mostraria só uma, com a outra viva e invisível. Pedido repetido troca o PIN do link ativo e devolve o mesmo slug — a URL já entregue continua valendo. Índice parcial não serviria: seed e teste criam vários links por equipe de propósito | Migration `0033`, `tests/11` bloco 4d |
| 26/08/2026 | **Recusa nas RPCs anônimas é `NULL`, nunca exceção — inclusive no caminho de escrita.** `public_daily_submit` sinalizava PIN errado com `raise … 42501`. PL/pgSQL não tem transação autônoma: a exceção aborta a transação que o PostgREST abriu e descarta junto o `update` do contador que `resolve_public_link` acabou de fazer. Os dois caminhos de leitura devolviam NULL e commitavam o contador; este não — um script anônimo varria os 10^6 PINs por `POST /rpc/public_daily_submit` sem nunca travar, com resposta de oráculo (403 no errado, 200 no certo). Não há conserto no ponto comum: um bloco `exception` é um SAVEPOINT e desfaria o incremento igual. A garantia é o contrato — nenhum chamador de `resolve_public_link` levanta na recusa — e o tripwire que cobra isso das três RPCs | Migration `0034`, `tests/11` blocos 6 e 6b |
| 26/08/2026 | **A tela trata `data === null` como falha.** É a outra metade da 0034 e vai no mesmo commit: só o banco seria pior que nada, porque a recusa passou a voltar como HTTP 200 com corpo `null` e o `DailyReport.tsx` só olhava `error` — a tela diria "🎯 Checkpoint concluído! +XP" para um envio que não gravou linha nenhuma | `src/pages/DailyReport.tsx` |
| 26/08/2026 | **Histórico de migrations do remoto reparado.** `supabase migration list` mostrava 25 migrations locais sem contrapartida remota (0001–0018 nunca registradas; 0019–0025 registradas com timestamps de outra ferramenta) e 7 versões órfãs no remoto — `db push` tentaria reaplicar a 0001. `migration repair --status applied` nas 25 locais + `--status reverted` nas 7 órfãs alinhou o histórico com o schema que já está no banco (os 18 marcadores de 0001–0018 foram conferidos objeto a objeto antes). Não roda DDL. `db push --dry-run` agora responde "up to date" | bookkeeping, sem migration |
| 26/08/2026 | **`daily-team-info`, `director-weekly` e `submit-daily-report` saíram do repo e do `config.toml`.** Estavam aposentadas desde 08/08 respondendo 410, mas enquanto o diretório existia o endpoint continuava publicado com `verify_jwt = false` — superfície anônima sem chamador nenhum. O frontend usa as RPCs públicas direto | `supabase/config.toml`, achado D05 |

| 26/08/2026 | **O leitor de planilha passa a ser `read-excel-file`; o `xlsx` (SheetJS 0.18.5) saiu.** O `xlsx` publicado no npm está abandonado na 0.18.5 e carrega CVE-2023-30533 (prototype pollution) e CVE-2024-22363 (ReDoS), sem correção no registro — e ele abria planilha de terceiro em dois lugares (importação de leads e remarketing do SDR). A outra saída era o build oficial do SheetJS, que corrige as duas CVEs mas só é publicado em `cdn.sheetjs.com`: teria diff zero, e em troca todo `npm i` (máquina de dev, CI, build da Vercel) passaria a depender de um host fora do registro, e o `npm audit` deixaria de enxergar o pacote — silêncio por invisibilidade, não por correção, e CVE futura do SheetJS não seria mais reportada. Ficou o pacote do registro: audit e dependabot continuam vigiando, `npm i` não muda para ninguém, e o chunk caiu de 333 kB para 66 kB. **Consequência aceita:** ninguém lê mais `.xls` (Excel 97-2003) — nem o `read-excel-file` nem o `exceljs`. O parser reconhece a assinatura do formato e responde "salve como .xlsx ou CSV" em vez de "formato não reconhecido". O `.csv` continua com o parser próprio escrito à mão, que não mudou | `importSheet.ts`, `SdrModule.tsx`, achado S06 |
| 26/08/2026 | **O SDR passou a usar o mesmo leitor da importação de leads.** Ele tinha um segundo parser copiado, e foi por isso que o upload de remarketing ficou sem os limites de 8 MB e 5.000 linhas que o `importSheet.ts` já aplicava — o mesmo arquivo era recusado numa tela e aceito na outra. Agora a planilha entra por `parseSheet` nos dois lugares e o SDR converte cabeçalho em objeto com `rowsToRecords`. Os limites continuam sendo a defesa contra arquivo grande travar a aba: a leitura segue na thread principal | `importSheet.ts`, `SdrModule.tsx` |
| 26/08/2026 | **Erro de banco na tela é `describeError`, e para isso o módulo de integração precisa lançar `dbError`.** Os nove módulos de `src/integrations/supabase/` relançavam com `throw new Error(error.message)`, o que descarta o `code` do Postgres: trocar só o toast faria toda falha cair na frase genérica — pior que hoje, porque parece corrigido. A ordem é módulo primeiro, tela depois. `leads.ts` e `checkin.ts` já eram assim desde a Tarefa D e serviram de referência | `supabaseError.ts`, integrações e telas, achado A05 |

| 27/08/2026 | **O piso tipográfico (X07) passa a ser 12 px, com UMA exceção escrita: 11 px em rótulo curto em caixa alta com `letter-spacing >= 0.1em`.** O X07 dizia "piso de 12 px" e os handoffs G e H o declararam cumprido — e estavam certos nos arquivos deles. O piso vazou pela `.text-eyebrow` do kit (11 px), que eles adotaram no lugar dos literais: toda tela voltou a ter de 7 a 17 elementos em 11 px e pelo menos 1 em 10 px (handoff-J §3.3). Duas saídas: subir a classe para 12 px, ou escrever a exceção. **Escolhida a exceção**, porque a classe existe exatamente para o caso que ela descreve — rótulo curto em caixa alta com tracking aberto é legível de um jeito que texto corrido de 11 px não é (caixa alta não tem descendente; o `letter-spacing` de 0.14em devolve a separação que o corpo menor tira). Subir para 12 px inflaria todo rótulo de seção do app por um número que ninguém verificou contra o desenho. **Consequência aceita:** o requisito deixou de ser um número só — é forma + número, e por isso precisou virar teste. `src/lib/type-scale.test.ts` reprova (a) qualquer `text-[Npx]` em `src/**`, com lista de exceções nomeada por dono, e (b) qualquer regra de `font-size` abaixo de `0.75rem` no `index.css` que não traga `text-transform: uppercase` e `letter-spacing >= 0.1em`, ou que desça abaixo de `0.6875rem`. Sem esse teste a exceção vira licença para 11 px em qualquer lugar e o problema volta em três sprints | `index.css`, `src/lib/type-scale.test.ts`, `docs/design-system.md` §3, achado X07 |
| 27/08/2026 | **`Badge` ganhou `size="sm"` — caixa menor, letra igual.** Catorze telas escreviam `text-[9px] h-4 px-1` à mão para caber um selo dentro de uma linha de lista; isso não eram 14 decisões, era uma. A alternativa era uma variante compacta com fonte própria, mas selo não é caixa alta e portanto não entra na exceção de 11 px: `sm` encolhe só o padding (`px-2 py-0`) e mantém `text-xs`. **Consequência aceita:** o selo cresce de 9 para 12 px onde estava menor — é o preço do piso, e era exatamente o texto que a varredura da J media como ilegível | `src/components/ui/badge.tsx`, 9 telas |
| 27/08/2026 | **No cabeçalho a 375 px, quem cede é o título da tela — e o que sai é o rótulo do papel, nunca o aviso de pré-visualização.** O `<p>` do título tinha `shrink-0` junto com `truncate`, e `shrink-0` anula o `truncate`: a caixa nunca encolhia, o excesso saía pela direita e o `overflow-hidden` do `header` cortava sino e avatar **em silêncio** — sem barra de rolagem para denunciar, que é por que a varredura da J achou "zero transbordo horizontal" com o sino inalcançável nas 16 combinações de 375 px. Corrigir só o `w-[150px]` do `RoleSwitcher` deixaria a mesma armadilha para a próxima tela de título comprido. A ordem de importância no estreito ficou escrita: **sino, avatar, e só então o seletor de papel**, que encolhe para ícone + seta. O aviso de que você está vendo como outra pessoa **não** some no celular — virou um selo curto ("prévia"), porque esconder isso no aparelho onde a tela é menor é pior, não melhor | `AppLayout.tsx`, `RoleSwitcher.tsx`, achado handoff-J §3.4 |
| 27/08/2026 | **O roteador fica no 6.x com o patch; migrar para o v7 é sprint própria, não item de segurança.** `react-router-dom@6.30.1` respondia por 3 das 10 entradas do `npm audit --omit=dev`, com severidade `high` e 5 GHSAs de redirecionamento aberto/XSS — e o app publica duas rotas anônimas (`/daily/<slug>`, `/diretor/<slug>`) numa URL que o cliente recebe. O bump explícito para `^6.30.6` (patch, mesma linha maior, zero mudança de código) puxa `@remix-run/router@1.23.4`, acima da faixa vulnerável `<=1.23.2`, e fecha 3 dos 5: GHSA-2w69-qvjg-hvjx (XSS via open redirect), GHSA-2j2x-hqr9-3h42 (`//` reinterpretado como URL protocolo-relativa) e GHSA-9jcx-v3wj-wh4m (redirecionamento externo por caminho não confiável). **Nem `npm audit fix` nem `--force` fazem esta troca:** o primeiro pula o bump (o resolvedor não considera a subida) e o segundo leva para `react-router-dom@7.18.2`, que é mudança maior. **Consequência aceita:** sobram 2 moderate que só têm correção no v7, e nenhuma das duas alcança este app. GHSA-337j (injeção de construtor em `deserializeErrors`) é caminho de hidratação de SSR — o app monta com `createRoot` + `<BrowserRouter>` declarativo, sem `hydrateRoot`/`StaticRouter`/`createStaticHandler` em `src/`, e o código nem chega ao bundle. GHSA-wrjc (open redirect por barra invertida em `<Link>`/`useNavigate`) exige destino não confiável, e existe **um** `navigate()` com valor não literal no app inteiro — `NotificationBell.tsx:104`, `navigate(resolveLink(item.link))` — já atrás da lista de permitidos da Tarefa M, que recusa `//host`, `\host`, `https://host`, `javascript:` e caractere de controle. Validar o destino é a defesa certa para um valor que vem da coluna `notifications.link`; remendo de biblioteca não substitui isso | `package.json`, `NotificationBell.tsx` (Tarefa M), `handoff-O.md` |
| 27/08/2026 | **`npm audit --omit=dev` neste repo não separa produção de build: 6 dos 7 achados restantes eram build disfarçado de produção.** `postcss`, `nanoid`, `yaml`, `glob`, `minimatch` e `brace-expansion` apareciam sob `--omit=dev` por uma única aresta — `tailwindcss-animate` está em `dependencies` e declara `tailwindcss` como peer, então o npm segue o peer e arrasta a árvore inteira do Tailwind, que é ferramenta de build e nunca vai para o navegador. O único achado genuinamente de produção era o `lodash`, que entra por `recharts` e **está** no bundle publicado (`charts-*.js`); o `recharts` importa `lodash/omit`, uma das funções citadas no aviso de prototype pollution. Os sete foram fechados por `npm audit fix` **sem** `--force`. **Não movi `tailwindcss-animate` para `devDependencies`**: a reclassificação é correta e o build da Vercel instala dev, mas mexer nisso na véspera de publicar troca um relatório enganoso por um risco de instalação. Fica anotado — enquanto ele estiver em `dependencies`, "N achados em produção" nessa saída precisa ser lido com a árvore de dependências na mão | `package.json`, `handoff-O.md` §3 |

| 01/09/2026 | **A linha de chegada deixou de ser a demonstração e passou a ser o aplicativo todo funcional** — todas as telas, funções, fluxos e perfis de usuário. Isso reclassifica o trabalho: o que era "pendência da próxima sprint" com 🟡 no placar vira escopo, e o critério de pronto deixa de ser "o cliente consegue percorrer o caminho" e passa a ser "o usuário clica e o resultado certo acontece". **Consequência aceita:** o que nunca foi começado e depende de terceiro (gestão de campanhas Meta, King Host, IA de voz) continua fora — não por decisão de escopo nossa, mas porque não temos como concluir sem eles | `PLANEJAMENTO.md`, `sprint-demo.md` |
| 01/09/2026 | **"Vencimento" era duas coisas e passou a ter dois nomes: `lead vencido` e `atividade vencida`.** Eu descrevi "atividades com vencimento" e o entendimento foi de que se tratava do lead que volta para a fila — mecanismos independentes que não se tocam. Lead vencido é automático, do sistema, e o corretor só perde a vez; atividade vencida é um compromisso que a pessoa marcou para ela mesma dentro de um lead ou negócio, e nada acontece sozinho quando vence. O termo virou entrada de glossário porque a colisão custou uma rodada inteira de conversa e vai voltar | `CONTEXT.md` (novo) |
| 01/09/2026 | **Atividades ganham tela própria.** A tabela de tarefas com data de vencimento existe, e o painel de criar já está montado nos modais de lead e de negócio — o que não existe é o lugar de **olhar**. Hoje, para lembrar do que marcou, o corretor teria que abrir negócio por negócio. A alternativa era considerar o recurso pronto e deixar como está: rejeitada, porque compromisso que vence sem ninguém ver não é agenda, é dado morto. A tela lista atrasadas, vencendo hoje e da semana, com recorte por equipe para o gerente. **Consequência aceita:** mais uma entrada no menu principal, que já tem 12 | `PLANEJAMENTO.md` ata 14/07 item 11 |
| 01/09/2026 | **A meta global ganha tela de cadastro.** Era a única regra de negócio do sistema que ainda exigia alguém rodar SQL na mão — o Dashboard lê a meta e mostra "—" enquanto a linha não existir. Fecha a pendência operacional nº 1 desta lista | `Equipes.tsx` ou tela própria |
| 01/09/2026 | **Meta Ads sai do `hidden: true` no menu.** Com a linha de chegada em "todas as telas funcionais", tela escondida é contradição — e é justamente onde o Douglas vai colar as credenciais da Meta. **Consequência aceita:** ele abre a tela antes de ter token e ela aparece sem dado; resolve-se com estado vazio dizendo que falta credencial, não escondendo a tela | `navigation.ts`, `MetaAdsSetup.tsx` |
| 01/09/2026 | **Os documentos do cenário passam a ter arquivo de verdade no Storage.** `deal_documents` tinha 69 registros apontando para arquivo e o Storage tinha **1 objeto**: 68 botões "Baixar" entregavam nada. O código de assinatura e download estava correto o tempo todo — o defeito era do seed. A alternativa era apagar os registros: rejeitada, porque some da tela um fluxo que funciona. **De passagem, o placar estava errado em dois pontos** e foi corrigido: a renomeação automática por `naming_pattern` está implementada e testada, e o botão de download também | `supabase/seeds/`, `PLANEJAMENTO.md` ata 23/07 itens 1 e 2 |
| 01/09/2026 | **O aprovador da conferência documental continua sendo o gerente do rateio do negócio, não o gerente da equipe do corretor.** O cliente pediu "quem valida é o gerente do corretor", e medimos: nos 34 pares corretor×negócio de hoje, o gerente da equipe está entre os gerentes do negócio em **34** — zero divergência. Mas isso é propriedade dos dados, não regra: o formulário deixa nomear qualquer gerente no rateio. Mudar a autorização é migration e cria o risco de travar negócio com rateio entre equipes. **Consequência aceita e registrada:** se um dia um corretor da equipe A entrar num rateio só com gerentes da equipe B, quem aprova não será o chefe direto dele | migration `0028`, sem alteração |

| 02/09/2026 | **O seed da demonstração só era idempotente dentro do mesmo mês e do mesmo dia — dois defeitos que só apareceram ao aplicar.** Nenhum agente os viu porque nenhum agente aplica seed; apareceram na primeira execução real depois da virada do mês. **(1) Temporada:** o BLOCO 7 encerra a temporada aberta de um mês anterior e depois abre a do mês corrente **com o id fixo `89000000-…-0001`** — o mesmo que acabara de encerrar. O `on conflict do nothing` engolia a colisão de chave primária e o arquivo seguia **sem nenhuma temporada aberta**, com `current_game_season()` nulo derrubando o INSERT de `game_events` 150 linhas adiante, por violação de not-null. O comentário logo acima do INSERT já previa exatamente esse risco (*"com `on conflict do nothing` o erro passaria despercebido"*); a chave fixa é que o realizava. Correção: o id fixo só é usado enquanto está livre, senão `gen_random_uuid()`; o `on conflict do nothing` saiu (silêncio aqui foi o que custou a depuração); o rótulo passou a sair de `season_label_ptbr` (0035) em vez do array embutido, e uma trava de sanidade falha com a causa escrita se não houver temporada aberta ao fim do bloco. **(2) Diário:** `daily_reports` e `daily_entries` entravam com id fixo e `on conflict (team_id, report_date)`, que **não cobre a chave primária** — rodar em outro dia muda `report_date` mas não o id, e o INSERT estourava. O id passou a carregar a data (`8f000000-0000-0000-AAAA-MMDD0000000N`), mantendo o prefixo `8f000000-` de que o rollback 069 depende. **Consequência aceita:** o cenário do diário passa a criar um id por dia; o 069 continua limpando tudo pelo prefixo | `seeds/060_demo_showcase.sql` |
| 02/09/2026 | **A 0037 travou o rótulo de esteira mas não retroalimentou quem já estava no CCA — corrigido pela 0050.** O trigger `cca_cases_sync_esteira_label` dispara na **transição** para `under_review`; os casos que já estavam lá antes da migration nunca transicionaram. Medido depois de aplicar: 3 casos em `under_review`, **0 negócios rotulados**. O efeito era o oposto do pretendido — a tela deixou de permitir marcar "foi à esteira" à mão e também não mostrava quem tinha ido de verdade. A 0037 já estava aplicada, e a regra do projeto é não editar migration aplicada: entrou a **0050**, idempotente, com a mesma precedência do trigger (rótulo de encerramento ganha do rótulo de esteira) para não existirem duas verdades sobre quando sobrescrever | migration `0050`, `docs/sprints/sprint-demo.md` |

| 02/09/2026 | **A 0037 quebrou toda escrita em `deals`, e a correção óbvia teria matado a própria proteção — corrigido pela 0051.** A 0037 revogou `deal_status_bare(text)` de `public, anon, authenticated`. Só que `deals_guard_esteira_label()`, o gatilho que roda em **todo insert e update de `deals`**, chama essa função e é `SECURITY INVOKER`: em Postgres o gatilho não exige privilégio sobre si mesmo, mas exige sobre o que ele chama, com o privilégio de quem disparou. Todo usuário autenticado que criasse ou editasse negócio levava `42501: permission denied for function deal_status_bare`, que o PostgREST devolve como **403**. Nenhuma revisão pegou: os agentes validam com `typecheck`/`lint`/`vitest`, e isso só aparece com sessão autenticada de verdade contra o banco. Quem pegou foi a **suíte E2E na homologação: 8 testes falhando com "o modal do negócio não fecha"** — criar, editar, perder pelo Status 2, rateio, matriz de etapas. Não era a tela; era a escrita inteira. **A correção que eu quase fiz estava errada:** tornar o gatilho `SECURITY DEFINER` (como o irmão `cca_cases_sync_esteira_label`) faria `current_user` virar o dono dentro dele — e a condição de escape do próprio guard é `current_user not in ('postgres','service_role')`. A trava passaria a liberar todo mundo, sempre: a suíte ficaria verde com a proteção da 0037 morta, que é apagar o teste em vez do defeito. A 0051 dá `grant execute ... to authenticated` em `deal_status_bare`, que é normalização de texto pura (`regexp_replace(upper(btrim(...)))`) — sem tabela, sem dado, sem privilégio. A revogação foi defesa sem ameaça e custou a escrita de `deals`. `anon` fica de fora: a superfície anônima continua sendo só as três RPCs do diário. **Provado nas duas pontas** com sessão simulada de corretor: o insert volta a passar (`can_edit_deal` verdadeiro) e o rótulo escrito à mão continua recusado com 42501 | migration `0051`, `0037` |

| 02/09/2026 | **O mesmo defeito de semente apareceu quatro vezes: id fixo com dado que depende do mês.** Depois do diário e da temporada vieram os **aportes de marketing** e as **metas**. Nos dois, o `period` já era `month_start(current_date)` — correto — mas o **id era fixo**: a linha do mês anterior ocupava a chave primária, o `on conflict do nothing` engolia a colisão e o mês corrente nunca nascia. Medido em 02/09: último aporte em 2026-08-01 e **9 metas, todas em agosto**. O efeito nas metas era o mais visível: o cartão do Dashboard voltava a mostrar "—", que é justamente a pendência que o bloco existe para fechar. Todos os quatro passaram a carregar a data no id (`8f000000-0000-0000-AAAA-MMDD…` / `8b000000-0000-0000-AAAA-MM…`), preservando os prefixos de que o rollback 069 depende. **A lição, para a próxima semente:** id fixo só é seguro quando a linha também é fixa; havendo `current_date` no corpo, ele precisa entrar na chave, senão o `on conflict do nothing` transforma "não consegui" em "não fiz nada" — sem ninguém ver. Depois da correção: 9 metas no mês corrente, meta global de 14 vendas, 2 aportes de setembro | `seeds/060_demo_showcase.sql` |

| 02/09/2026 | **O corretor podia arrastar o próprio negócio para "Aprovado", e o gerente não — o inverso da regra de crédito. Corrigido pela 0052.** Medido na matriz `stage_permissions`: `broker/approved` com `can_enter = TRUE` e `manager/approved` com `false`. Aprovar é decisão da esteira de crédito, e é por isso que `deals_guard_stage()` (0028) recusa a entrada em `approved` sem a conferência documental aprovada — **duas travas para o mesmo fato discordavam**: a do banco recusava, a da matriz permitia. Quem lia a tela via o caminho aberto e só descobria no erro, e "Aprovado" é número que a diretoria olha. Não é regressão desta sprint: `e2e/broker/etapas.spec.ts` ("a matriz realmente nega 'Aprovado' ao corretor") cobra isso desde antes, não foi tocado hoje, e vinha reprovando — foi a suíte E2E na homologação que trouxe o defeito à tona. Nenhuma migration semeia `stage_permissions` (as linhas entram pela tela de Permissões), então a 0052 **corrige o estado sem criar trava nova**: um admin continua podendo reconceder pela tela, que segue sendo a fonte de verdade da matriz. Depois: CCA, admin e diretor entram; corretor e gerente não | migration `0052`, `stage_permissions` |

| 02/09/2026 | **O seed recriava o link de diretoria SEM PIN — a homologação foi corrigida à mão em 27/08 e o arquivo ficou divergindo dela em silêncio.** `seeds/040_reports_game_workspace.sql` criava `seed-diretoria-daniela` com `pin_hash` nulo, enquanto os dois links de equipe logo acima já nasciam com `extensions.crypt('123456', ...)`. Consequência: todo `db:reset` num banco limpo reabria exatamente o buraco que a decisão de 26/08 (migration 0033) mandou fechar — o slug do link de diretoria é derivado do nome, então quem soubesse o nome do diretor montava a URL e lia a diretoria inteira. Achado pela frente `checkpoint-anonimo` da rodada 12, que precisou confirmar por que o spec falhava. Corrigido no seed. A segunda pendência levantada junto (**"talvez não exista caminho na tela para gerar PIN de link de diretoria"**) **não procede**: `AdminDailyTeams.tsx:316` já chama `issuePin` com `kind: "director_checkpoint"`, e os 4 links da homologação têm PIN | `seeds/040_reports_game_workspace.sql` |

| 02/09/2026 | **O SDR podia cadastrar negócio manual e levar 100% do rateio de VGV — e a 0048 afirmava por escrito que isso era impossível. Fechado pela 0053.** A 0048 dizia: *"Papel sem correspondência (partner, sdr, marketing) não chega aqui: `deals_insert` recusa"*. Era falso, e a frente de pipeline da rodada 12 provou com um teste que grava pelo JWT real do SDR + chave anônima (sem service_role, com RLS e gatilho valendo): esperava 403, recebeu **201**. A cadeia: `handle_new_auth_user` (0002) concede `broker` a **todo** perfil novo e nunca o retira, então um SDR é `{sdr, broker}`; o `with check` de `deals_insert` era `has_any_role(admin, director, manager, broker, cca)` e o `broker` de cadastro o fazia passar; o gatilho então o escolhia como CORRETOR, e daí saem o rateio (`recalc_deal_shares` divide entre brokers) e os pontos de `'venda'` (`deals_award_points`). **Fechar só o gatilho criaria defeito novo:** sem linha em `deal_participants`, `can_edit_deal()` é falso e `deal_clients_insert` a exige — o SDR gravaria o negócio e tomaria 42501 no cliente logo depois, trocando rateio roubado por negócio órfão. A 0053 decide pelo **papel efetivo** (`auth_effective_role`, mesma precedência de `primaryRole` no front) nos **dois** pontos: policy e gatilho. `nulls first` no `order by` é a trava para o futuro — papel novo no enum e ausente do array vira o efetivo e é reprovado nas duas listas: falha fechado, em vez de virar corretor por omissão, que é como este defeito nasceu. **Conferido antes de aplicar:** `convert_lead_to_deal` e `claim_lead` são SECURITY DEFINER, então o caminho legítimo do SDR (converter lead) continua aberto; e dos 24 perfis da homologação **ninguém perdeu acesso** — SDR, marketing e sócio já eram recusados ali. O buraco era para frente, com todo usuário criado pelo app. **Consequência deliberada:** quem for `{broker, sdr}` de verdade perde a criação manual, porque o par é indistinguível de um SDR comum; o caminho é o admin retirar o papel de SDR em Permissões — decisão visível, no lugar do rateio silencioso | migration `0053`, `e2e/admin/pipeline-negocio.spec.ts` |

| 04/09/2026 | **O cofre aceitava qualquer coisa em qualquer campo — e aceitou.** Medido em `private.integration_credentials`: `brevo/sender_email` guardava a **mesma string de 89 caracteres começando com `xkey`** que estava em `brevo/api_key` — a chave de API colada duas vezes. O envio do dossiê falhava na Brevo com "remetente inválido", o erro morria no log da edge function, e a tela do cofre continuava dizendo "configurado". Como o cofre **nunca devolve o valor gravado** (garantia de que o segredo não trafega para o navegador), a tela de salvar é a **única fronteira** que enxerga o que a pessoa colou — e não validava nada. O catálogo ganhou `formato` (`email`, `url`, `digits`, `token`) e `validarCredencial`; a tela recusa antes de gravar, com mensagem que diz o que se esperava. Marcados só os três campos de forma conhecida: `brevo/sender_email` (e-mail), `supabase/functions_url` (URL, e exige `https` — `http` exporia a service role key em trânsito) e `meta/whatsapp_phone_number_id` (dígitos). **Chave de terceiro fica sem validação de propósito:** inventar regra de prefixo reprovaria credencial boa no dia em que o provedor mudar o formato. 9 testes em `integrationCatalog.test.ts`, sendo o primeiro exatamente o caso que aconteceu. **O dado errado continua no banco** — o valor certo é do dono (precisa ser um domínio verificado no Brevo) e está na tabela de bloqueantes | `integrationCatalog.ts`, `AdminIntegrations.tsx` |
| 04/09/2026 | **Rodada 13: 87 itens do inventário de conclusão, 12 frentes em paralelo, média de 72% para 95%.** O inventário anterior (101 itens, 12 auditores + cético) mediu a média em 66% e separou o que falta em `fazivel`, `terceiro` (credencial de fora) e `decisao`. As ~50 decisões vieram com recomendação analisada; **os agentes implementaram a recomendação** em vez de devolver a pergunta, seguindo a ordem do dono de não ser interrompido com o que dá para decidir — só voltaram as irreversíveis, as que mexem em comissão e as que dependem de um fato que só ele sabe. 15 migrations (`0054`–`0069`), 9 edge functions redeployadas com as travas de autenticação provadas depois (401 nas seis com JWT, 403 no webhook da Meta com token errado). Os testes unitários foram de **296 para 575** | 15 migrations, `sprint-demo.md` |

| 05/09/2026 | **"Troca de perfil não funciona" (relato do cliente) era uma fonte de verdade dividida em duas.** `useAuth()` devolvia `roles` com os papéis **reais** enquanto `isAdmin`, `can()` e `canEnterStage()` já respeitavam a prévia. Metade das telas remendava na mão (`const effectiveRoles = previewRole ? [previewRole] : roles`, repetido em 8 arquivos) e a outra metade esquecia — **Pipeline, Checkpoint, Atividades, SDR, Automação de Leads e o editor de negócio** continuavam mostrando a tela de administrador durante a prévia. Agora `roles`/`role` **são** os efetivos e quem precisa da verdade pede `realRoles`/`realRole`. Só dois lugares precisam: o próprio seletor (com os efetivos, `roles.includes('admin')` viraria falso na prévia, o controle sumiria da tela e o admin ficaria **trancado** nela) e a tela de Configurações, que mostra quem você é. As 8 cópias da regra foram apagadas. **Consequência aceita e já anunciada na própria tela:** a prévia troca menu e botões, não os DADOS — o RLS continua respondendo pelos papéis reais, então uma tela pré-vista como corretor traz os números do admin. 3 testes novos em `AuthContext.test.tsx`, incluindo o de quem não é admin não conseguir se promover pela prévia | `AuthContext.tsx`, `RoleSwitcher.tsx`, `Settings.tsx`, +7 |
| 05/09/2026 | **Menu: quatro grupos viraram dois.** Pedido do cliente — "tudo que for de configuração no mesmo menu". O principal tinha treze itens, com *Links* e *Resultados* no meio do caminho de quem trabalha, e ainda existiam *Administração* e *Sistema* separados sem nenhum critério que dissesse de qual dos dois era "Dados". Ficou **Menu principal** (o que se usa para trabalhar) e **Configurações** (todo o resto: Resultados, Permissões, Integrações, Construtoras, Diário — Links, IPs, Automação, Meta Ads, Dados e a própria conta). Nenhuma rota mudou — link salvo continua chegando — e a visibilidade segue saindo de `ROUTE_PERMISSION`, item a item. Junto veio um conserto de acessibilidade que o rearranjo tornou visível: `SidebarGroup` é uma `div` sem papel nem nome, então o rótulo do grupo era **só pintura** e quem usa leitor de tela ouvia uma lista corrida de links. Agora `role="group"` + `aria-labelledby`. O teste do corretor deixou de cobrar a ausência do grupo (ele **tem** "Configurações", que é a própria conta) e passou a cobrar o que interessa: o grupo aparece com **um** item só | `navigation.ts`, `AppSidebar.tsx`, 2 specs |
| 05/09/2026 | **A extração do Pipeline virou `.xlsx` e virou permissão (0092).** Duas coisas erradas no mesmo botão. (1) O CSV mentia em silêncio: toda célula saía entre aspas, então VGV, percentual e dias chegavam ao Excel como **texto** — somar a coluna de VGV do mês dava zero e o `SOMASE` de comissão por corretor também. A planilha existe justamente para conferir comissão e precisava de um "texto para colunas" antes de servir. Em `.xlsx` o número viaja como número, com o cabeçalho preso no topo. Dependência nova: `write-excel-file` (mesma família do `read-excel-file` que a importação já usa; o instalado só lê), carregada por `import()` dinâmico para não pesar no pacote inicial de quem só quer ver o quadro. (2) O botão era de **todo mundo que abre o Pipeline** — e corretor, SDR e marketing abrem. O arquivo leva VGV, percentual de rateio e VGV **por corretor** do recorte inteiro: a folha de comissão da operação num anexo de WhatsApp. Pedido do cliente: só admin e sócio. A trava é `pipeline.export` na matriz, não uma lista de papéis no código, porque quem administra permissões já tem onde mudar sem deploy. **Limite declarado:** a trava é de tela — a planilha é montada no navegador a partir do que o RLS já entregou; o que a permissão remove é o caminho de um clique, não o acesso ao dado | migration `0092`, `exportacao.ts`, `Pipeline.tsx` |
| 05/09/2026 | **Atalho de check-in fora do Pipeline** (pedido do cliente). `CheckinQueueBar` repetia a fila de `/checkin` no topo da tela de negócios, e a tela própria continua lá, no menu. Arquivo copiado para `_recuperacao-faceimob/2026-09-05/` antes de sair | `Pipeline.tsx`, `pipeline/index.ts` |
| 05/09/2026 | **Duas execuções de E2E ao mesmo tempo destroem uma à outra — e o sintoma acusa o produto.** Descoberto ao rodar dez agentes em paralelo contra a homologação: os dez usuários da suíte têm identidade **fixa** e o `global-teardown` os apaga, então a faxina de uma execução remove as contas e as duas equipes que a outra ainda está usando. O resultado foram 8 falhas que pareciam regressão de hierarquia e organograma (`team_leader_names` vazio, colunas sumindo) e eram ambiente. `scripts/e2e-lock.mjs`: `mkdir` atômico em `os.tmpdir()`, derivado do caminho do repositório, com PID e batimento para que uma execução morta (Ctrl+C, `taskkill`) não deixe a trava presa. Quem chega depois **espera a vez**, com aviso. Só protege quem entra por `npm run e2e`; `npx playwright test` na mão passa por fora | `scripts/e2e-lock.mjs`, `scripts/e2e.mjs`, `e2e/README.md` |
| 05/09/2026 | **O 500 no cadastro de e-mail duplicado não era bug de código: era código que nunca foi implantado.** `provision-broker-user` já tinha a pré-checagem que devolve 409 com `existing_profile_id` — no arquivo local. A versão **no servidor** era anterior e caía no `catch` geral, devolvendo 500 com a mensagem crua do GoTrue (que, medido, não contém palavra nenhuma que identifique "e-mail duplicado": volta `Error updating user` com status 500). Duas horas de diagnóstico foram gastas procurando defeito onde não havia. Deriva entre repositório e servidor é invisível e não tem alarme: entrou `npm run functions:deploy` para não repetir | `package.json`, deploy da function |

| 05/09/2026 | **Rodada 17: 10 zonas de arquivos disjuntos, 40 agentes, revisão adversarial em duas lentes por zona.** Cada zona entregou, foi revisada por um agente de correção/segurança e um de UX/a11y — os dois instruídos a REFUTAR — e refinada com os achados que se sustentaram. Somando as dez: **95 achados, ZERO de afrouxamento de proteção** (nenhum agente deixou um teste verde enfraquecendo asserção, seletor ou validação), 87 aplicados e 29 recusados com evidência. Testes unitários de 819 para **889**; typecheck e lint limpos; 4 migrations novas (`0087`–`0089`, `0092`–`0093`). O detalhe de cada zona vem nas linhas abaixo | 40 agentes |
| 05/09/2026 | **Busca de lead trazia gente demais, não de menos.** Duas coisas, separadas.  1) O defeito que eu corrigi (provado com medição): a cláusula de telefone entrava sempre que o termo tivesse 3 dígitos EM QUALQUER LUGAR — inclusive num e-mail ou num nome. `leadSearchFilter` usava `term.replace(/\D/g,"")` e `matchesFilters` usava `digits.length > 0`, as duas sem perguntar se o que foi digitado é um telefone. Medido na homologação: `select count(*) from leads where phone ilike '%8101%'` → 12 linhas de 74. Logo, procurar "joao8101@gmail.com" disparava `phone.ilike.*8101*` e devolvia 12 leads de estranhos, e o filtro da tela os MANTINHA porque usava a mesma regra frouxa. Num cadastro de milhares, buscar o e-mail de um cliente devolve uma parede de gent `searchPhoneDigits(term)` novo em `leads.ts`: devolve os dígitos só quando o termo é um telefone (sem letras: apenas dígitos, espaço, parêntese, +, ponto e hífen) e tem 3 dígitos ou mais; devolve `null` para texto. É a fonte única da regra. `leadSearchFilter` passou a montar `phone.ilike.*<digitos>*` a partir de `searchPhoneDigits` em vez de `term.replace(/\D/g,"")` — termo de texto não vira mais busca por telefone. **Decisões:** A regra de 'isto é um telefone' virou UMA função exportada (`searchPhoneDigits`) e não uma condição repetida nos dois lugares. Porquê: a busca tem duas camadas obrigatoriamente (o banco recorta, o cliente peneira por status/origem/corretor/grupo) e qualquer divergência entre elas aparece para o operador como 'o sistema achou e escondeu'. Consequênc · Letra no termo desqualifica o telefone, em vez de tentar adivinhar o número dentro do texto. Porquê: é a regra mais simples que separa '(11) 98877-0001' de 'joao8101@gmail.com', e o custo do erro é assimétrico — um telefone escrito com letra no meio é raro, uma busca por e-mail é rotina. Consequência: quem colar 'Tel: (11) 98877-0001' no campo não  **Revisão adversarial (2 lentes):** 7 achados, NENHUM de afrouxamento de proteção; 5 aplicados no refino, 2 recusados com evidência. **Fica pendente:** O E2E `e2e/admin/importar-planilha.spec.ts` NÃO está verde: 9/10. A falha restante é o passo da busca, mas está provado que não é o filtro (banco e função pura conferidos com o número exato). A hipótese do modal do `NewLeadNotifier` não foi FECHADA com artefato porque `test-results/` é apagado por c | leads.ts, model.ts, model.test.ts |
| 05/09/2026 | **A recusa por construtora era dita duas vezes.** Duas mensagens diferentes com o mesmo começo, visíveis simultaneamente. `dealRequiredError` (guards.ts:157) devolve "Escolha a construtora: sem ela o negócio não entra na conferência documental." e `projectPlaceholder` (guards.ts:179) devolvia "Escolha a construtora antes". O placeholder aparece assim que o formulário abre — todo negócio novo nasce sem construtora, o Select de Empreendimento nasce desabilitado e mostra o placeholder. A recusa aparece ao clicar em "Criar negócio". Os dois nós coexistem, `getByText(/escolha a construtora/i)` casa com ambos e o Playwright estoura strict mode violation. Provado de duas formas: (1) inspeção do código — `grep -rni "scolha a construtora" src/` isol guards.ts · `projectPlaceholder`: o primeiro ramo deixou de dar ordem e passou a descrever a dependência — "Escolha a construtora antes" virou "Depende da construtora". O campo está desabilitado; ele diz de que depende, não repete o comando do campo obrigatório. DealDetailModal.tsx · `handleSave` chama `dealRequiredError(form)` ANTES de `onSave` e retorna sem toast; a frase passa a ser renderizada dentro do formulário. Estado `tentouSalvar` (boolean) + `developerError` derivado na renderização (`tentouSalvar ? dealRequiredError(form) : null`), então escolher a construtora apaga o aviso na hora, sem efeito e sem segundo clique. **Decisões:** Tratei como defeito de produto, não de seletor: o teste continua com `getByText` sem `.first()`, e o teste unitário novo CONTA os nós. Consequência: se alguém reintroduzir a duplicação, os dois níveis reprovam — o strict mode do Playwright e o `toHaveLength(1)` do vitest, este último em 3 s e sem depender do banco de homologação. · Quem mudou de texto foi o PLACEHOLDER, não a recusa. A recusa é a única frase que tem direito à ordem imperativa ("Escolha a construtora: …"), porque ela nomeia o campo obrigatório e o motivo (a conferência documental depende de `developer_id`). O placeholder descreve um campo desabilitado — "Depende da construtora". Consequência: o rótulo continua **Revisão adversarial (2 lentes):** 8 achados, NENHUM de afrouxamento de proteção; 5 aplicados no refino, 4 recusados com evidência. **Fica pendente:** A suíte E2E remota é um recurso único e disputado: com ~8 agentes rodando em paralelo o GoTrue da homologação devolveu `429 over_request_rate_limit` no `globalSetup` em 6 tentativas seguidas (o preparo cunha 10 sessões por execução). A 7ª tentativa passou. Não é defeito do código — mas quem rodar a  | guards.ts, guards.test.ts, DealForm.tsx |
| 05/09/2026 | **A sonda do Brevo passou a dizer QUAL remetente serve.** `_shared/brevo.ts`: nova `listSenders(apiKey)` — leitura pura de `GET /v3/senders` que devolve `{ok:true, senders:[{email, ativo}]}` ou `{ok:false, status, error}`. `fetch` dentro de try/catch (`status: 0` = a chamada nem saiu) para que falha de rede não vire 500 da sonda. O parse descarta qualquer item cujo `email` não passe em `senderEmailProblem`. `submission-dispatch/index.ts` (`probeBrevo`): reordenado. A consulta a `/v3/senders` vem ANTES do veredito do remetente; 401 continua parando a sonda (veredito sobre a chave); qualquer outra falha do provedor não derruba a sonda — ela responde o que já respondia. Todas as respostas de recusa por remetente passaram a carregar `remetentes`. **Decisões:** A consulta a `/v3/senders` foi movida para ANTES do veredito sobre `brevo/sender_email`. Porquê: é justamente quando o valor gravado não presta que o admin precisa da lista — a ordem antiga garantia que ele nunca a visse. Consequência: uma sonda com chave válida e remetente lixo agora custa uma chamada HTTP a mais, e devolve o caminho de saída em v · 401 e as demais falhas de `/v3/senders` são tratadas de forma diferente. 401 é veredito sobre a chave e para a sonda (a Brevo recusou a credencial). Qualquer outro status, ou erro de rede, NÃO derruba a sonda: ela segue e responde exatamente o que responderia sem a consulta. Consequência: instabilidade da Brevo nunca transforma 'a credencial está b **Revisão adversarial (2 lentes):** 10 achados, NENHUM de afrouxamento de proteção; 10 aplicados no refino, 5 recusados com evidência. **Fica pendente:** A conta da Brevo tem 11 remetentes verificados e a maioria é caixa de pessoa (fabio@, joice.m@, inajara@, maria.fernanda@, thayse.oliveira@, roberto.mendes@, mauricio.vieira@, archimedes@). Escolhi controle@faceimob.com.br por ser institucional, mas quem manda no assunto é a operação — se preferirem | brevo.ts, index.ts, integrationCatalog.ts |
| 05/09/2026 | **Remarketing: trava com prazo, ritmo respeitado e dedupe.** Três causas distintas, todas provadas antes da correção. (1) Duplicidade: unique (list_id, phone) da 0008 + normalize_phone + importação atômica da 0031 — provado por bloco DO na homologação, que devolveu duplicate key value violates unique constraint "remarketing_contacts_list_id_phone_key" para as máscaras "(11) 98888-2001" e "11988882001"; o app não deduplicava porque comparava a string crua. (2) Trava eterna: a soltura da trava dependia de um catch que o runtime não deixa rodar quando mata a function no teto de tempo — o lote de 500 com 250 ms de rate limit garante ultrapassá-lo. (3) Credencial: a function já respondia 503 com code/credential, mas a tela descartava o code e só emitia toa Dedupe de telefone na importação: lerContatos() colapsa linhas cujo telefone normaliza igual (mesma regra de normalize_phone), mantém a PRIMEIRA ocorrência e o toast diz quantas linhas repetidas ficaram de fora. Telefone impossível continua indo ao banco para o trigger recusar — a atomicidade e o aviso 'telefone inválido' não mudaram. Trava de disparo com prazo (10 min): a edge function assume trava vencida por um segundo update condicional (status=running AND updated_at < limite), e a tela usa disparoEmAndamento() para reabilitar o botão e parar de exibir 'Disparando…' numa lista abandonada. Removida a checagem de list.status anterior ao update — ler-e-depois-gravar deixava janela para o se **Decisões:** Não construí montagem de público a partir dos leads do CRM, e a razão é de segurança, não de esforço. As policies de leads (medidas na homologação) são leads_select (assigned_to em auth_visible_profiles, ou fila com leads.view_queue) e leads_select_sdr (admin/sdr/marketing SOMENTE para lead que já está numa conversa de SDR). Um operador de marketin · Dedupe no cliente e não no banco: a alternativa era ON CONFLICT DO NOTHING dentro de import_remarketing_list (migration 0086, que estava reservada para mim). Não usei, porque o banco engoliria a linha em silêncio e a tela nunca saberia quantas sumiram — e porque mexer na RPC atinge também o caminho de telefone inválido, cuja recusa em bloco é decis **Revisão adversarial (2 lentes):** 14 achados, NENHUM de afrouxamento de proteção; 14 aplicados no refino, 0 recusados com evidência. **Fica pendente:** A function sdr-whatsapp-broadcast NÃO foi reimplantada na homologação. A versão publicada (v6) ainda é a anterior. Não implantei porque o deploy por MCP exige colar as ~450 linhas inline e um caractere perdido derruba também os caminhos human_reply e probe, que outras telas usam — e porque, sem cred | remarketing.ts, remarketing.test.ts, RemarketingTab.tsx |
| 05/09/2026 | **Template com variável inventada não salva mais.** `WhatsAppTab.save()` passou a BARRAR a gravação: nome fora do formato da Meta e, para template ATIVO, qualquer problema de `templateIssues` (placeholder sem variável, variável sobrando, variável fora do catálogo). O toast diz o problema e a saída ('Corrija antes de salvar ou desmarque Template ativo para arquivá-lo'). `templateVars.nameIssue(name)`: nova função pura que devolve o motivo de o nome não casar com um template aprovado na Meta (só [a-z0-9_], máximo 512) ou `null`. Campo vazio devolve `null` de propósito — quem cobra o obrigatório é o salvar, para o formulário novo não abrir em vermelho. **Decisões:** A trava vale só para template ATIVO. Bloquear sempre deixaria o operador sem saída: um template já gravado quebrado (como o `boas_vindas_faceimob` do seed) não poderia nem ser desligado, e desmarcar 'Template ativo' é justamente o que o diálogo de exclusão recomenda para tirar de circulação sem perder o cadastro. Os dois envios (`meta-ads-webhook`  · Bloqueio no clique, não botão desabilitado. Botão `disabled` sem motivo visível é pior de usar e de ler por leitor de tela; o Salvar continua alcançável e o toast + a caixa de avisos dizem o problema e a saída. Consequência: quem clica sem ler recebe o motivo em vez de um botão morto. **Revisão adversarial (2 lentes):** 10 achados, NENHUM de afrouxamento de proteção; 11 aplicados no refino, 3 recusados com evidência. **Fica pendente:** E2E não executado: os 2 casos novos e o ajuste da fixture estão escritos, tipados (`tsc -p tsconfig.e2e.json` limpo) e lintados, mas as 4 tentativas morreram no `globalSetup` com 429 (rate limit de auth do Supabase, saturado pelas execuções paralelas). Precisa ser rodado quando o alvo estiver livre  | 20260907870000_0087_whatsapp_template_nome_formato.sql |
| 05/09/2026 | **Aviso de prazo: um por estouro, e fila honesta.** Dois defeitos, cada um provado antes de ser corrigido.  (1) IDEMPOTÊNCIA. Causa: o gatilho era `after update of released_at ... for each row` sem cláusula `when`, e a única barreira contra o aviso duplicado era o recorte `released_at is null` que cada um dos cinco escritores (`assign_lead`, `claim_lead`, `close_lead`, `reassign_lead`, `release_expired_leads`) repete por conta própria. Prova, executada e desfeita por rollback na homologação: `update public.lead_assignments set released_at = now()` numa linha com `release_reason='timeout'` levou a contagem de `lead_lost_timeout` de 718 para 720. Depois da 0088 a mesma reescrita deu 718 → 718, e a transição legítima (`released_at` nulo → preenc Migration 0088: o gatilho `notify_lead_timeout` ganhou `when (old.released_at is null and new.released_at is not null)`. A função não mudou; o que mudou é QUANDO ela é chamada. Um estouro de prazo = um aviso, garantido no ponto por onde todo escritor de `released_at` passa. Migration 0088: `notification_queue_health()` passou a excluir `channel = 'in_app'`. O painel de fila do admin passa a contar só o que tem envio pendente — hoje, zero, com o estado vazio honesto em vez de 891 linhas de sino disfarçadas de mensagens represadas. **Decisões:** O gatilho ganha `when (old.released_at is null and new.released_at is not null)` em vez de um `if` novo dentro da função. PORQUÊ: a função já filtra `release_reason <> 'timeout'`; repetir o recorte de transição no corpo seria a mesma regra em dois lugares, e na cláusula `when` ela ainda evita a chamada de função quando não há o que fazer. CONSEQUÊN · Nada foi descartado da fila represada, porque não havia fila. PORQUÊ: a 0065 descartou as 77 linhas de julho e a 0083 instalou o corte de 2 h que roda a cada minuto; a medição de hoje deu ZERO pendentes com 788 já processadas. Escrever um novo descarte seria correr contra um problema que outro código já resolve. CONSEQUÊNCIA: a política de vencimen **Revisão adversarial (2 lentes):** 10 achados, NENHUM de afrouxamento de proteção; 5 aplicados no refino, 4 recusados com evidência. **Fica pendente:** A credencial da Meta Cloud API continua ausente no cofre (`meta/whatsapp_access_token` e `meta/whatsapp_phone_number_id`). Enquanto ela não chegar, o aviso por WhatsApp NÃO sai — o item fica na fila com o motivo escrito por até 2 h e depois é descartado com o motivo trocado por 'descartada por idade | 20260907880000_0088_aviso_prazo_idempotente.sql |
| 05/09/2026 | **Campanhas Meta: verba, período, pausar e copiar.** Migration 0089: `ad_campaigns` ganha `lifetime_budget` (verba contratada), `starts_on`/`ends_on` (período) e `lead_source_id` (FK para `lead_sources`, `on delete set null`), mais os CHECKs `ad_campaigns_lifetime_budget_not_negative` e `ad_campaigns_periodo_coerente`. Idempotente (`add column if not exists` + `pg_constraint`). Aplicada na homologação e provada com um DO block que tentou inserir período invertido e verba negativa — os dois recusados, zero linhas residuais. analytics.ts: nasce `problemaNaCampanha(input)`, a ÚNICA regra de recusa, chamada por `createAdCampaign`, `updateAdCampaign` e pelo formulário. Cobre ID externo vazio, nome vazio, status fora de ACTIVE/PAUSED, os três campos **Decisões:** NÃO tentei sincronizar com a Graph API. Exige token de sistema, escopo `ads_management` e revisão de app da Meta — dependência de terceiro que não temos, e a própria ata registra que a verificação de empresa do setor imobiliário limita a automação. Consequência: tudo o que a tela faz é registro LOCAL, e cada superfície de escrita (formulário, botão · `lifetime_budget` é coluna NOVA em vez de reaproveitar `total_spend`. Os dois respondem perguntas diferentes: verba contratada × gasto realizado, e é a diferença entre eles que faz alguém pausar. Consequência: uma coluna a mais; sem ela, "quanto ainda posso gastar" continuaria fora do sistema. **Revisão adversarial (2 lentes):** 9 achados, NENHUM de afrouxamento de proteção; 9 aplicados no refino, 2 recusados com evidência. **Fica pendente:** Sincronizar de verdade com a Meta continua fora: precisa de token de sistema com `ads_read`/`ads_management`, id da conta `act_…` no cofre e revisão do app. O botão "Sincronizar gasto com a Meta" segue desabilitado com o motivo escrito — de propósito, porque botão que erra em silêncio é pior. | 20260908890000_0089_ad_campaigns_gestao.sql |
| 05/09/2026 | **Documento sem arquivo deixou de oferecer download.** A causa não é o dado, é a ausência de uma pergunta. `deal_documents` (a linha) e `storage.objects` (o arquivo) são duas gravações independentes e nada no banco casa as duas: seed que só registra, rollback de upload que não conseguiu remover, objeto apagado por fora. Nenhum dos dois consumidores de documento perguntava ao Storage se o arquivo existe antes de oferecer a ação — então o mesmo defeito aparecia duas vezes, com consequências diferentes.  Provado assim: (a) SQL na homologação mostrou 2 de 3 `lead_attachments` sem objeto, com as linhas intactas — o buraco existe hoje e não só no passado; (b) `POST /storage/v1/object/sign/<caminho inexistente>` devolve 400/`NoSuchKey`, ou seja, a recu `documents.ts`: nova `missingStoragePaths(paths)` — ponto único que pergunta ao Storage quais caminhos não têm arquivo legível, via `createSignedUrls` (1 requisição para a lista toda, erro por caminho, passando pelas MESMAS policies de `storage.objects` que o download vai enfrentar). `DealDocumentUpload.tsx`: depois da carga, confere a lista no bucket. Documento sem arquivo deixa de ter botão "Baixar" — a linha fica vermelha com ícone `FileX`, o texto "· arquivo ausente", e no lugar do download aparece "Reenviar <nome>", que abre o mesmo seletor de arquivo do tipo. **Decisões:** Perguntar ao Storage com `createSignedUrls` (plural) em vez de `select` em `storage.objects` ou `list` por prefixo. PORQUÊ: é uma requisição para a lista inteira, dá erro por caminho, e passa pelas MESMAS policies que o download vai enfrentar — um `select` diria "existe" para um objeto que a pessoa não pode baixar, e o botão continuaria quebrado pa · `Set<string> · null` em vez de `Set<string>`, com `null` = não conferido. PORQUÊ: falha da própria conferência (rede, RLS do bucket) não pode acusar o dossiê inteiro de vazio nem travar o envio ao gerente a cada oscilação. CONSEQUÊNCIA: com a conferência indisponível a tela volta EXATAMENTE ao comportamento anterior (mostra "Baixar", conta o obriga **Revisão adversarial (2 lentes):** 9 achados, NENHUM de afrouxamento de proteção; 10 aplicados no refino, 3 recusados com evidência. **Fica pendente:** A TELA de anexos do lead continua com o mesmo defeito. Corrigi o DADO (`lead_attachments` está 3/3 no bucket), mas `src/components/LeadDetailModal.tsx:223` faz `window.open(await signedAttachmentUrl(...))` sem conferir existência e sem estado "arquivo ausente" — o buraco volta assim que um objeto su | documents.ts, documents.test.ts, DealDocumentUpload.tsx |
| 05/09/2026 | **SDR sem chave: a tela diz, em vez de fingir.** O requisito não estava quebrado no banco nem na function: estava quebrado na tela. Causa raiz provada por SQL — private.integration_credentials não tem linha do provider openai na homologação, então requireSecret('OPENAI_API_KEY') falha em todo turno — combinada com a leitura do código: o único ponto que expunha essa falta era o 503 do sdr-agent-chat, disparado depois de o operador digitar e enviar, e o botão 'Testar chave', visível só para papéis de escrita. Nada na abertura da tela consultava o cofre, então 'Ativo', o seletor do Playground e o vínculo agente-origem continuavam prometendo um agente que não responde. Correção na fonte estreita: um único sinal (action=status, um booleano) bus sdr-agent-chat ganhou action:'status' — devolve {configured:boolean} e nada mais. Fica ANTES da porta de papel de escrita (só depois de requireUserPermission('menu.sdr')), porque director/manager/partner abrem o módulo e também precisam saber que a IA está parada. Não chama a OpenAI (isso continua sendo o 'probe'), então é barato o bastante para rodar na abertura da tela. Deployada na homologação: sdr-agent-chat v9, verify_jwt=true preservado. types.ts: constante IA_SEM_CREDENCIAL com o que falta e onde cadastrar (Admin · Integrações, OpenAI — chave de API, mesmo rótulo de src/lib/integrationCatalog.ts). Fonte única para os três pontos que avisam. **Decisões:** O sinal de credencial vem de action:'status' na própria sdr-agent-chat, não de list_integrations. PORQUÊ: list_integrations exige a permissão settings.integrations, que sdr e marketing não têm — usá-la deixaria justamente quem opera o módulo sem o aviso. CONSEQUÊNCIA: quem tem menu.sdr passa a saber se existe credencial da OpenAI (um booleano, nunc · O campo do Playground continua aberto mesmo sem credencial. PORQUÊ: a chave pode ter sido cadastrada há um instante noutra aba, e 'Testar chave da OpenAI' ao lado confirma sem gastar turno; bloquear o campo troca um erro honesto por um beco sem saída. CONSEQUÊNCIA: o operador ainda consegue enviar e receber a bolha de erro — mas agora avisado antes **Revisão adversarial (2 lentes):** 10 achados, NENHUM de afrouxamento de proteção; 9 aplicados no refino, 5 recusados com evidência. **Fica pendente:** A chave da OpenAI continua ausente do cofre — bloqueio de terceiro, fora da missão. Enquanto ela faltar, nada prova o caminho fim-a-fim: o modelo respondendo, as tags saindo do texto de verdade, o handoff disparado por [QUALIFICADO], sdr_handoff carimbando o lead e assign_lead distribuindo. Assim qu | index.ts, types.ts, SdrModule.tsx |
| 05/09/2026 | **A documentação parou de mentir sobre o próprio projeto.** `.claude/CLAUDE.md`: 2 linhas. Migrations 18→79 (0001–0084, com aviso de que a numeração tem lacunas e conta-se arquivo), tabelas 58→62 com RLS nas 62, edge functions 11→9 mais `_shared/` (com os 5 arquivos nomeados), tests = 47 arquivos, seeds descritos como são (4 fases de catálogo 010–040 + 050 e 060 com rollback), legacy = 70. Plano ativo `plano-entrega.md` → `sprint-demo.md` + `decisoes.md`, com `plano-entrega.md`/`plano-100-funcional.md`/`sprint-01..05` marcados como histórico e datados. `README.md`: removido todo o template Lovable (cabeçalho, 'How can I edit', GitHub/Codespaces, deploy, domínio). Entrou identidade do projeto, 'Como rodar', 'Como validar' (com a armadilha do `tsc --no **Decisões:** **Tirei a chave publicável e o ref reais do `.env.example` e deixei exemplos com a forma preservada.** A chave é pública por construção (é a credencial anônima do PostgREST; quem barra é o RLS), então não é vazamento de segredo — mas valor real em arquivo versionado prende o repositório a um projeto só e declara qual é o ambiente. **Consequência qu · **Mantive `VITE_SUPABASE_PROJECT_ID` no `.env.example` em vez de apagar, mas marcado como não lido.** Nenhum `import.meta.env` o consome (grep em todo o repo), e apagar seria o diff menor. Só que ele é cadastrado na Vercel junto com os outros dois (`docs/prompts/D1-C-dados-demo.md:27`, `handoff-O.md:409`): apagar do template faria alguém esquecer d **Revisão adversarial (2 lentes):** 8 achados, NENHUM de afrouxamento de proteção; 9 aplicados no refino, 1 recusados com evidência. **Fica pendente:** **O grafo do `graphify-out/` continua desatualizado** — a auditoria da sprint listou junto com os demais, mas ele não está entre os meus arquivos e é gerado, não editado. Fecha com `graphify update .` depois que as frentes desta rodada assentarem; rodar agora só produziria um grafo que envelhece na  | CLAUDE.md, README.md, .env.example |

| 06/09/2026 | **A trava da suíte E2E se enforcou sozinha — e o conserto é sobre em que sinal confiar.** A primeira versão só considerava a trava abandonada quando o PID do dono estava morto **E** o batimento parado. Medido: a rodada de agentes terminou, o dono morreu, o número do PID foi reusado por um dos 43 processos node da máquina, `process.kill(pid, 0)` respondeu "vivo" para sempre, e a execução seguinte **esperou os 60 minutos inteiros sem rodar um teste**. Quem manda passou a ser o BATIMENTO: uma execução viva o reescreve a cada 30 s, então batimento parado há mais de 3 min só acontece se o dono morreu — o PID não acrescenta nada a isso e mente quando é reciclado; ficou como atalho (PID inexistente dispensa esperar o batimento vencer). Junto foi fechada a corrida do nascimento: entre o `mkdir` e a primeira escrita de `dono.json` há uma janela de milissegundos em que um segundo processo lia "sem dono" e tomava a trava — agora a idade da PASTA desempata. Provado: o cenário exato que travou 60 min é assumido em **7 ms** | `scripts/e2e-lock.mjs` |
| 06/09/2026 | **Aviso de lead na fila deixou de sequestrar a tela.** Os dois avisos do `NewLeadNotifier` abriam o mesmo `Dialog` modal, e eles não são a mesma coisa: `assigned` tem cronômetro correndo e um "Atender agora" que perde a validade sozinho — interromper é o certo; `queued` oferecia "Depois" e "Abrir leads", nada que não coubesse num aviso. O custo do modal para o caso informativo foi medido: enquanto ele está aberto o Radix marca TODO o resto da página com `aria-hidden`, então quem usa leitor de tela **perde a tela inteira** por causa de um comunicado — e o Playwright, pelo mesmo motivo, para de achar qualquer papel ARIA, que era a causa provável da falha intermitente no teste de cadastro de lead. Havia ainda o absurdo diário: o admin que acabava de digitar um lead recebia um modal anunciando a chegada do lead que ele mesmo cadastrou (`leads` não tem coluna de autor, então não dá para reconhecê-lo pela linha — com o aviso em toast o problema deixa de existir). O `use-toast` ganhou `action` para o destino continuar a um clique | `NewLeadNotifier.tsx`, `use-toast.ts` |
| 06/09/2026 | **Enxugamento visual pedido pelo cliente ("um pouco poluído"), com uma correção de responsividade junto.** Saíram: o quadrado colorido atrás de cada ícone (eram 7 manchas azuis na régua do Dashboard e 6 nas seções, disputando atenção com os números), a sombra colorida e o `-translate-y` do hover (passar o mouse fazia sete cartões pularem um a um), e a sombra permanente do destaque — destaque virou UMA coisa, a borda. Respiro de 12→16 px entre cartões e 24→32 px entre blocos. O ranking do Pipeline foi de ~350 px (pódio de pedestais, a primeira dobra inteira) para ~130 px de lista; o pódio continua na Gamificação, que é onde ele é o assunto. **Achado no meio disso:** o Dashboard tinha 13 px de rolagem horizontal a 375 px, vindos de um elemento invisível — a tabela acessível dos gráficos usava `sr-only` na própria `<table>`, e `display:table` IGNORA o `width:1px` da regra, então ela ficava com 352 px, absoluta, empurrando a página. Movido para um `<div>` em volta (que respeita largura e `overflow`); medido de novo: **0 px** | `KpiCard.tsx`, `SectionCard.tsx`, `PipelineTopRanking.tsx`, `ChartData.tsx` |
| 06/09/2026 | **Ranking do Pipeline por papel** (decisão do dono). Corretor vê SÓ a posição dele — ele já tem o pódio completo em Gamificação, e no meio do trabalho o que interessa é onde ele está; gerente e diretor veem o top 3 do que lideram; admin e sócio, o da empresa. O recorte dos DADOS continua sendo do servidor (`visible_game_ranking`): a tela escolhe o que mostra do que já chegou, nunca o contrário. Papel desconhecido cai no recorte mais estreito — **falha fechada**, porque papel novo no enum é exatamente o caso em que ninguém lembra de voltar no arquivo, e o padrão não pode ser "publica o placar da empresa" | `PipelineTopRanking.tsx` + 4 testes |
| 06/09/2026 | **Login: o nome passou a bater com o que chega.** A tela pedia "código de 6 dígitos" e o que o Supabase envia, sem o template `magic_link.html` publicado, é um LINK — quem abria o e-mail achava que o envio tinha falhado. Agora o caminho anunciado é o que existe ("clique no botão, o link já autentica, vale 1 hora") e o campo de seis dígitos ficou atrás de "Recebi um código de 6 dígitos", para o dia em que o template for publicado. Apagar o campo trocaria um defeito por outro | `Login.tsx`, `login.anonimo.spec.ts` |

| 06/09/2026 | **Voltei atrás na 0093 — sócio deixa de ser promovido a administrador automaticamente (migration 0094).** A 0093 leu "admin e sócio têm as permissões iguais, é só porque o cliente quer outro nome" e deu o papel `admin` a todo `partner`, por gatilho. Funcionava. O que ela apagava de passagem foi medido DEPOIS: `partner` não é um papel vazio esperando um nome — é um observador de LEITURA AMPLA e ESCRITA NENHUMA, e essa fronteira está provada em **15 asserções, em 12 arquivos** de `supabase/tests/` ("sócio não edita lead", "sócio não apaga cliente", "sócio não cadastra IP", "o sócio não escreve no lead alheio", "sócio não enumera a corretagem", "a tela dele é somente leitura" na esteira…). Com a 0093 as quinze passam a afirmar o contrário do que o banco faz. **Não reescrevi as quinze** porque a frase do dono aceita duas leituras e o custo de errar é assimétrico: se ele quis um papel só com dois nomes e eu ficar no estreito, custa UM clique (marcar também "Administrador" na ficha); se ele quis só o nome e eu promover todo sócio, a operação ganha gente podendo trocar papéis, mexer na matriz, fechar mês e apagar dado, sem ninguém ter pedido. O pedido do NOME continua atendido no front (`roleLabelFor`): quem tem `partner` lê "Sócio", com ou sem `admin` junto. Reaplicar a 0093 é uma migration, se a leitura certa for a outra | migration `0094`, `permissions.ts` |
| 06/09/2026 | **Permissão nova nasceu inerte e o tripwire pegou.** A 0092 criou `pipeline.export` e eu não a registrei em `src/lib/featurePermissions.ts` — o mapa que diz, ao lado de cada interruptor da tela de Permissões, QUEM lê aquele código. Resultado: o switch aparecia com o selo "Ainda sem efeito", prometendo ao admin que ligar não muda nada, quando muda. Quem reprovou foi `e2e/admin/permissoes.spec.ts`, contando 4 códigos inertes onde deviam existir 3 — o teste existe exatamente para isso e funcionou. Junto entrou a permissão no `supabase/seed.sql` (catálogo e concessões de admin e sócio): sem isso o código dependia da ordem migration × seed num `db:reset` | `featurePermissions.ts`, `seed.sql`, `featurePermissions.test.ts` |
| 06/09/2026 | **Seletor de E2E apertado, não afrouxado.** `getByRole("option", { name: /^E2E Corretor/ })` casava três opções — "E2E Corretor", "E2E Corretor Rival" e "E2E Corretor 3". O reflexo errado seria `.first()`: ele escolheria um corretor qualquer e o assert seguinte ("o lead vai para o corretor escolhido") passaria por acaso. Trocado por nome inteiro com `exact: true` | `e2e/admin/importar-planilha.spec.ts` |
| 06/09/2026 | **Inventário de dois repositórios do cliente, para decidir o que integrar.** (1) `doughgomes/faceimob` **não é um painel de corretores** — é o site imobiliário público, em outro projeto Supabase (`xbydnzenakqrousxuggk`). Tem cadastro de corretor (`createOne` em `src/lib/broker.functions.ts:130`) e o contrato é e-mail + nome + telefone + **senha**; temos os três primeiros, e senha é justamente o que o nosso CRM não tem por desenho (entra por link no e-mail). Três defeitos anotados no código de lá: senha guardada em TEXTO PURO (`broker_credentials.initial_password`), busca de usuário existente limitada à primeira página de 200 (`listUsers({page:1,perPage:200})` — acima disso o cadastro quebra), e ausência de endpoint HTTP (é *server function* do TanStack, exige sessão de admin do site). (2) `dg-jarvis-os`: 47 migrations, 48 edge functions. **A Evolution API NÃO existe lá** — `evolution-whatsapp` é uma casca marcada "DEPRECATED" que repassa para a Cloud API oficial; a decisão de usar Evolution será construída do zero aqui. O que vale importar está ranqueado por valor ÷ custo na resposta ao dono; o item 1 é sincronizar gasto/verba/status da Meta em modo LEITURA, que é o único buraco declarado do nosso painel de Marketing (`CampaignPerformancePanel.tsx:608` já tem o botão desabilitado com o motivo escrito e `ad_campaigns.synced_at` esperando alguém escrever) | inventário, 8 agentes |

| 06/09/2026 | **As 7 falhas da suíte, separadas por causa — e três delas não eram defeito.** (a) Duas eram minhas e já descritas acima (texto do login, `pipeline.export` sem entrada no mapa). (b) TRÊS eram **carga da máquina**: `allowed-ips`, `roleta` e `revisao-documental` estouraram os 45 s enquanto 8 agentes do inventário disputavam CPU; com a máquina quieta passaram sem tocar em nada. Vale como método: timeout no relógio da suíte não é diagnóstico, é sintoma — reproduzir isolado veio antes de qualquer conserto. (c) Duas eram **o produto certo e a fixture errada**: `revisao-documental` e `cca/fluxo-completo` inseriam a linha em `deal_documents` sem NUNCA subir o arquivo, e a rodada 17 fez a tela conferir o armazenamento — documento sem arquivo deixou de contar como obrigatório cumprido, então "Enviar ao gerente" e "Enfileirar envio" ficam desabilitados, **com razão**. A fixture passou a subir bytes de verdade. **Consertar isso quebrou um terceiro teste, e o nome da variável avisava**: `docSemArquivo` em `cca/leitura-e-envio` quer um documento SEM arquivo, para a tela poder dizer "Documento sem arquivo" e travar o envio. Virou opção (`semearDocumento(..., { comArquivo: false })`) em vez de regra fixa — os dois cenários são reais e agora os dois têm teste. `subirArquivo` saiu de duas cópias e foi para `e2e/helpers/negocio.ts` | `helpers/negocio.ts`, `cca/esteira.ts`, 3 specs |
| 06/09/2026 | **O que a Meta recusou, em português — e por que código desconhecido NÃO ganha tradução.** Disparo recusado gravava `JSON.stringify(data)` em `remarketing_contacts.last_error`, e quem opera lia `{"error":{"message":"(#131047) Re-engagement message"}}`, ou seja, "deu erro". `_shared/metaErros.ts` traduz os códigos que aparecem de fato numa operação brasileira (janela de 24 h, entrega recusada, conta restrita por país, template inexistente ou com variáveis a mais, token expirado, limites de ritmo) com título, explicação e PRÓXIMO PASSO. **Código fora do catálogo devolve `null` de propósito**: inventar explicação plausível para um código novo manda a operação para o lado errado com ar de certeza — pior do que mostrar a mensagem crua da Meta, que é feia e verdadeira. Cada erro ainda diz se **vale repetir** (limite de ritmo passa sozinho; janela de 24 h e template errado não passam nunca, repetir só gasta tentativa). Adaptado do `dg-jarvis-os`, não copiado: o original embute um telefone fixo e o nome do outro produto. A tela de contatos já mostrava `last_error`, então o front não mudou. 9 testes | `_shared/metaErros.ts`, `sdr-whatsapp-broadcast` |
| 06/09/2026 | **Nenhum ajudante de edge function tinha teste — e o motivo era uma linha de configuração.** `vitest.config.ts` incluía só `src/**`, então parsers e tradutores que rodam em produção (o `parseTags` da resposta do agente de SDR, entre outros) seguiam sem um único teste, e a rodada 17 registrou isso como bloqueio concreto. `supabase/functions/**/*.test.ts` entrou no `include`. Limite real: só carrega arquivo SEM dependência do runtime do Deno — quem importa `secrets.ts` (que lê `Deno.env`) continua fora do alcance. Foi por isso que `metaErros.ts` nasceu sem `import` nenhum | `vitest.config.ts` |
| 12/09/2026 | **Avisos (toasts) no meio da tela, maiores, com cor e som — substitui o B10.** Pedido do dono: notificação mais chamativa, com som quando algo dá certo ou falha, no meio da tela e maior. Agora TODO aviso (sucesso, erro, aviso, info e neutro) sai no centro — é o `position` do Toaster, e quem passar outra posição na chamada continua vencendo —, com 440 px de largura (até 600 px segue ocupando a largura da tela), título maior, ícone e anel na cor do tipo (`success`, `destructive`, `warning`; info só com anel fino) e botão de fechar. Sucesso toca um som curto que sobe; erro, um curto, grave, que desce. Aviso, info e neutro ficam mudos, e o mudo do header vale para os dois. **Consequência:** erro no meio da tela pode cobrir o campo que a pessoa está corrigindo, que era o motivo do B10 deixar o erro no canto; a mitigação é o botão de fechar e a duração de 5 s. **Diálogo aberto (corrigido na revisão):** com diálogo modal o Radix desliga o clique no resto da página e o aviso herdava isso; em diálogo estreito (`max-w-sm`, 384 px) o X do aviso de 440 px fica sobre o overlay, e clicar nele FECHAVA o diálogo e perdia o formulário (agendar visita, reatribuir lead). Agora a lista de avisos tem `pointer-events-auto` e o `DialogContent` não conta clique dentro do aviso como clique fora: o X fecha só o aviso. **Som em dobro:** som de aviso cede a vez — não toca se outro som foi pedido há menos de 1,2 s do fim dele (rajada de avisos toca uma vez; aviso logo depois do som de lead fica mudo) e é abafado se uma comemoração começa enquanto ele soa. A faixa de premiação não entra nessa regra. De passagem, pelo CSS gerado: o centro do B10 usava `-translate-y-1/2`, que reescreve o `transform` e apagava o `translateX(-50%)` do sonner — no desktop o aviso saía com a borda esquerda no meio da tela; trocado por `top: calc(50% - altura/2)` | `ui/sonner.tsx`, `use-toast.ts`, `lib/engagement/audio.ts`, `SoundPreview.tsx`, `sonner.test.ts`, `audio.test.ts` |
| 12/09/2026 | **Gestor ampliava o próprio alcance pela API — três portas, fechadas pela 0128.** Achado na auditoria de visibilidade por papel e provado na homologação (carga real) com o JWT do gestor, em bloco DO desfeito por exceção: (1) `team_members_manage` (0044) conferia a permissão e a equipe, nunca QUEM entra — o gerente incluiu na própria equipe um corretor sem equipe e os leads visíveis foram de 7.825 a 10.108 (negócios 815 → 894), já editáveis; (2) `teams_admin_write` (0068) só exigia `director_id` = o diretor — ele criou equipe com um corretor qualquer de gerente e foi de 30.021 a 32.304 leads; (3) `reassign_lead` conferia o corretor de destino e não o lead de origem — o gerente puxou para a equipe dele um lead que não enxergava (0 → 1). **Regra única:** gestor só grava hierarquia e só move lead sobre gente que ele JÁ enxerga, por `auth_visible_profiles()`. Filiação exige `profile_id` visível; equipe gravada pelo diretor exige gerente nulo ou visível; `reassign_lead` usa o predicado da `leads_select` e responde "Lead não encontrado." (P0002) fora do alcance, para não confirmar que o id existe. **Por que não "só admin"** (a sugestão inicial, alinhada à 0104): o diretor cria equipe em `/admin/daily-teams` (`menu.admin_daily_teams` é dele) e fechar `teams` quebraria a tela — o seletor de gerente dela lê `profiles` pelo RLS e só oferece quem a trava aceita —; e `teams.manage` seguiria no catálogo sem efeito, arrastando selo, mapa e E2E sem fechar nada a mais. O que o switch governa agora é DESLIGAR integrante. Nenhuma tela de gerente ou diretor grava `team_members` (só `Equipes`/`BrokerEditModal`, admin desde a 0104). **Depois de aplicar:** as três chamadas são recusadas e os leads visíveis não mudam; continuam passando gerente realocando lead da fila e de membro, gerente desligando membro, diretor renomeando a própria equipe e criando equipe sem gerente, admin incluindo qualquer um. Reabrir filiação de quem já saiu passou a ser recusado. **Limites** (revisão adversarial em três lentes; o arquivo de teste foi simulado inteiro na homologação em bloco desfeito e toda asserção passou pelo motivo certo): (a) o diretor que reativa uma equipe inativa DELE recupera o alcance sobre as filiações que ficaram abertas nela, sem checar quem são — o `ponytail` da migration fala em "sem gerente", mas vale também com gerente visível; 0 casos hoje, e o diretor não monta o cenário sozinho; (b) herdado da 0068 e mantido: o diretor ADOTA equipe órfã cujo gerente ele já enxerga e herda todos os membros dela; órfã só nasce da exclusão de um diretor (0 hoje), e fechar exige reverter a decisão da 0068 — adoção só pelo administrador; (c) mover corretor entre duas equipes do mesmo gestor são dois requests e a inclusão na nova é recusada, porque ao fechar a antiga ele sai do alcance; hoje nenhuma tela chega lá (`menu.equipes` é só de admin e sócio), e `setTeamByManager` passou a conferir as linhas da reabertura e dizer que o vínculo anterior não voltou, em vez de deixar a pessoa sem equipe em silêncio. `./scripts/validate-schema.sh --all` e o E2E NÃO rodaram nesta rodada (sem Docker na máquina); a regressão ficou no teste SQL novo e no E2E reescrito, que afirmava a própria brecha (esperava 201 do gerente incluindo um corretor de fora) | `0128_gestor_sem_ampliar_alcance.sql`, `tests/99_hierarquia_escopo.sql`, `tests/21_feature_permissions.sql`, `e2e/admin/permissoes.spec.ts`, `lib/featurePermissions.ts`, `seed.sql`, `supabase/people.ts` |
| 12/09/2026 | **Diretor e gerente só veem o que é deles (0141).** Regra do dono: corretor vê os leads que recebeu; só sócio e admin veem tudo; gerente e diretor, o que se relaciona com eles; CCA segue com a esteira inteira. `can_read_all()` virou `is_admin()`: o diretor passa a ver os negócios da própria hierarquia (7.579 → 2.947 na homologação; admin, sócio, gerente, corretor e CCA com md5 idêntico da lista visível). Fila: marketing, admin e sócio ficam com a inteira; gerente e diretor, com a geral e as roletas onde há gente da hierarquia deles — vale para ler, editar, anexar, distribuir e realocar (`reassign_lead` da 0128 acompanhou o ramo da fila; a trava de origem continua). A policy da fila lê colunas da linha, porque criar lead sem dono com RETURNING falhava até para o admin com a versão por conjunto. Diretor só grava filiação em roleta que já alcança; atribuições e o aviso `lead_unattended` seguem o mesmo alcance. Storage separa ler, gravar e apagar, com a trava do dossiê enviado. Métricas de marketing somam só o visível fora marketing/admin/sócio; `existing_lead_phones` aceita até 1.000 por chamada; `distribution_queue` e `lead_distribution_group` exigem alcance; `sdr-agent-chat` confere lead e conversa (deploy v13). `deals.view_all` segue inerte, com texto honesto em Permissões. Provas: B e positiva da 0128 repetidas depois da 0141; teste 99 inteiro na homologação em bloco desfeito | `0141_visibilidade_diretor_fila.sql`, `tests/99_visibilidade_diretor_fila.sql`, `tests/14`, `tests/59`, `sdr-agent-chat`, `dashboard/data.ts`, `leads.ts`, `featurePermissions.ts`, `NewLeadNotifier.tsx`, `e2e/director/dashboard.spec.ts` |
| 12/09/2026 | **Game: distrato, reabertura, rateio e congelado (0142).** O −600 sai quando o negócio vira perdido e o corretor tem a venda dele na temporada aberta, mesmo com etapa aberta no meio; ganhar de novo retira o distrato (rastro em `deal_history`); venda já paga numa temporada encerrada, sem distrato lá, não paga de novo. Entrar no rateio traz esteira, aprovado e incompleto que o negócio já gerou; sair leva todos. Envio à construtora externa pontua esteira. `close_game_season` congela a própria `visible_game_ranking` (só corretores, só quem pontuou). A temporada importada homônima virou "Setembro 2026 (importada)". Diário diz que os pontos dele não são os do ranking; o filtro semanal diz que conta pela data em que o ponto entrou. **Consequência:** venda de temporada encerrada (legado Bubble incluído) perdida agora não penaliza | `0142_game_distrato_reabertura.sql`, `tests/99_gamificacao_distrato_reabertura.sql`, `DailyReport.tsx`, `Gamification.tsx`, `ReopenDealDialog.tsx` |
| 12/09/2026 | **Push do navegador — servidor (0143, 0144, 0145, 0147).** O push viaja na própria linha `in_app` do sino: `push_subscriptions` e `push_preferences` com RLS da pessoa, `push_category()` como regra única (lead_recebido, lead_prazo, lead_atividade, crédito — ligadas por padrão —, outros — desligada), gatilho por comando + cron `faceimob-push-dispatch` acordando a edge `push-dispatch` (VAPID + aes128gcm só com WebCrypto, vetor da RFC 8291 no vitest). Reivindicação do lote e `push_done_subs` impedem aviso em dobro; aparelho só sai por 404/410 ou 10 recusas. Destinatário só grava `read_at`; `push_test` só pela RPC, 1 a cada 30 s. Novos avisos ao dono do lead: comentário, atividade, visita e crédito feitos por outra pessoa; WhatsApp do lead no máximo 1 a cada 10 min. 0144: aviso de cron conta só execução terminada com falha (os 24 `cron_failure` falsos foram marcados como lidos; 0 falhas em 48 h). **0145: o par VAPID é gerado dentro da edge (`{"acao":"garantir_vapid"}`) e gravado por RPC só de service role — ninguém viu a chave privada.** 0147: `search_path` fixo nas auxiliares (linter). **Provado de ponta a ponta:** Chrome real assinou no FCM, `send_test_push` gravou, a edge respondeu `sent: 1` e o `sw.js` mostrou a notificação; dados do teste apagados | `0143`…`0145`, `0147`, `functions/push-dispatch`, `_shared/webpush.ts`, `_shared/secrets.ts`, `tests/99_push_notificacoes.sql`, `integrationCatalog.ts` |
| 12/09/2026 | **Push do navegador — aparelho.** `public/sw.js` (sem cache) + `lib/push.ts` + seção "Avisos no celular e no computador" em Configurações: ativar/desativar por aparelho, 5 categorias, "Enviar teste", "ligado" só com o registro confirmado no servidor. Logout apaga o endpoint e fecha as notificações antes do signOut; "Encerrar todas as sessões" aborta se não limpar os aparelhos. Com o app focado no layout logado só "Lead recebido" não vira notificação do sistema (tem popup); o resto sempre aparece. iPhone só com o app na Tela de Início. Electron e navegador sem push: notificação do sistema pelo realtime do sino, com as mesmas preferências | `public/sw.js`, `lib/push.ts`, `integrations/supabase/push.ts`, `notifications/PushSettings.tsx`, `Settings.tsx`, `NotificationBell.tsx`, `AuthContext.tsx`, `main.tsx`, `manifest.webmanifest` |
| 12/09/2026 | **Âmbar como única cor de destaque.** Botão `highlight` (âmbar, texto escuro, brilho parado só no escuro) em Atender agora, no primeiro Atender do funil, em Fazer check-in, no Entrar e no popup motivacional; Fechar mês voltou a contorno. Âmbar em texto e traço usa `gold` (4,85:1 no claro): número do KPI destacado, nomes da venda fechada, barra/percentual da faixa do game, 1º lugar do pódio sem pulso, item ativo do menu, borda do turno ativo. Tom `live` no StatusBadge só em Game ativo e Turno ativo; o cronômetro do lead aguardando atendimento é neutro (vermelho no último minuto) em funil, aviso, detalhe e tabela. `glow-pulse` removida; contraste de ouro travado no theme-contrast | `button.tsx`, `index.css`, `tailwind.config.ts`, `StatusBadge.tsx`, `KpiCard.tsx`, `NewLeadNotifier.tsx`, `LeadFunnel.tsx`, `LeadDetailModal.tsx`, `LeadsTable.tsx`, `Pipeline.tsx`, `SaleCelebration.tsx`, `PipelineTopRanking.tsx`, `Podium.tsx`, `Checkin.tsx`, `AppSidebar.tsx`, `Login.tsx`, `MotivationalPopup.tsx`, `Gamification.tsx` |
| 12/09/2026 | **Sons: desktop por protocolo próprio e meta batida.** O Electron abria `dist` por `file://` e ficava em branco; agora serve `app://faceimob` com fallback para index.html e bloqueio de traversal, e ganhou o AppUserModelID `br.com.faceimob.crm` para a notificação do Windows. Interruptor de som sincroniza entre abas; card de venda dura o trecho da música (7 s) para a venda seguinte não ficar muda; com menos movimento ou faixa recusada toca o som curto do marco. "Meta batida" dispara uma vez por pessoa, temporada e mês quando o VGV cruza a meta individual (hoje há 0 metas cadastradas). Prévia ganhou a música de premiação. Removidos `lib/sound.ts`, `playLeadAlert` e `playSaleFanfare` | `electron/main.cjs`, `useSomDePremiacao.ts`, `lib/engagement/audio.ts`, `EngagementLayer.tsx`, `SoundPreview.tsx`, `SoundToggle.tsx` |
| 12/09/2026 | **Automação de Leads respeita o alcance do diretor e atividade só para quem enxerga (0146).** A tela mostra a quem não é admin/sócio só as roletas de `auth_distribution_group_ids()`, trava o que está fora com o motivo e avisa que tirar o último da equipe pode tirar a roleta do alcance (`lib/roletaAlcance.ts`, com teste). 0146: o WITH CHECK da `tasks_write` exige responsável em `auth_visible_profiles()` (SDR pode para o dono de lead que lê); antes qualquer um mandava atividade — e, com o push, texto livre — para o celular de qualquer um. **Consequência aceita:** o autor não fecha atividade de quem saiu do alcance dele; o responsável e o admin fecham. **Validação:** `validate-schema.sh` e E2E NÃO rodaram (Docker não sobe nesta máquina; E2E remoto exigiria tirar a service role do cofre). No lugar: testes 99 da 0141, 0142, 0143 e 0146 rodados inteiros na homologação em bloco desfeito, typecheck limpo e vitest 109 arquivos / 1.354 testes | `AdminLeadAutomation.tsx`, `lib/roletaAlcance.ts`, `0146_atividade_so_para_quem_enxerga.sql`, `tests/99_atividade_para_quem_enxerga.sql` |
| 13/09/2026 | **Toast em toda ação do usuário, num padrão só.** Pedido do dono: aviso em quase tudo que o corretor faz (salvar, alterar lead, atividade, mover etapa — inclusive Primeiro contato na aba Leads do Pipeline) e padrão no sistema todo. Inventário de ~250 ações por área, implementação, revisão adversarial e crítico de cobertura. **Padrão:** sucesso `toast.success("<Objeto> <particípio>")` curto (2,5 s em ação pequena e frequente: arrastar card, marcar atividade, switch); erro `toast.error("Não foi possível <verbo> <objeto>", { description: describeError(...) })`; resultado válido que não é erro vira `toast.warning` mudo ("Ninguém para receber agora"); lote vira um aviso agregado ("3 leads fora da sua mão"); navegação, filtro e leitura não ganham toast. **Duplicados fora:** atender lead e check-in já avisam pelo EngagementLayer; venda com corretor tem o card. **Aviso falso corrigido na causa:** `setTaskStatus`/`setVisitResult` não pediam a linha de volta, e o sócio/CCA via "Atividade concluída" com a RLS recusando em silêncio — agora `select("id")` e erro sem linha (teste `activities.write.test.ts`). `functionErrorMessage` deixa de mostrar a frase em inglês do SDK de functions (teste próprio). **Consequência:** muitos sucessos antes mudos agora tocam som (rajada toca um só). E2E ajustados ao texto novo e não rodados (alvo remoto exige a service role no ambiente) | `src/pages/*`, `components/leads/*`, `components/pipeline/*`, `TaskPanel.tsx`, `VisitPanel.tsx`, `LeadFunnel.tsx`, `LeadDetailModal.tsx`, `activities.ts`, `functionError.ts` |
| 13/09/2026 | **Travamento no menu lateral e na navegação — causas medidas e corrigidas.** (1) O menu no hover animava a largura do espaçador por 500 ms e repintava a página inteira a cada quadro: agora no hover o painel abre POR CIMA do conteúdo (Ctrl+B continua empurrando), itens memorizados, sem tooltip no celular. (2) O `Suspense` único do App trocava a tela inteira por "Carregando..." na 1ª visita (921 ms no Pipeline): agora o fallback é só no miolo e as telas são pré-carregadas no ocioso. (3) `.animate-fade-in` do AppLayout terminava com `both` e deixava `transform` preso na tela: o Chrome repintava as 1.000 linhas a cada tique do cronômetro — virou `backwards` (mesma entrada). (4) Cronômetro de 1 s isolado na célula (a tabela de Leads não re-renderiza mais) e lista montada em lotes de 100. (5) `listLegacyDeals` em páginas paralelas e cache compartilhado entre Pipeline, CCA e Dashboard; kanban memorizado (redesenho de 2.288 cartões → 0); cálculo morto de ~9 milhões de comparações no card de ranking removido; realtime do Pipeline agrupado. (6) AuthContext estável na volta à aba e logout limpa o cache; canal de lead novo não é refeito a cada clique; telas de gestão usam cache do React Query. **Número errado achado de passagem:** o PostgREST corta em 1.000 linhas — a esteira CCA e o total de leads do Dashboard contavam 1.000; agora certos. A lista de leads do Dashboard continua amostra dos últimos 1.000, com legenda honesta. Medido com build de produção e sessão falsa (Playwright, CPU 4x): menu no hover 942 ms → 156 ms; 20 s parado em /leads 8,5 s → ~2 s bloqueados; maior tarefa ao abrir Leads ~3 s → 0,5 s. **Ainda pesa em máquina lenta:** layout da tabela de 1.000 linhas a cada segundo com lead em trava (~100–390 ms/s em CPU 4x) — saídas: `table-layout: fixed` (muda o visual) ou virtualizar (Ctrl+F deixa de achar). Busca de leads com ILIKE faz varredura de 102 mil linhas (1,2–5,4 s): pede RPC + índice trigram, não feito | `ui/sidebar.tsx`, `AppSidebar.tsx`, `AppLayout.tsx`, `App.tsx`, `index.css`, `AuthContext.tsx`, `newSchema.ts`, `pipeline/*`, `LeadsTable.tsx`, `LeadFunnel.tsx`, `Leads.tsx`, `EngagementLayer.tsx`, telas de gestão |
| 13/09/2026 | **Roleta independente de quem a acionou (0148) e disco do Docker.** A 0141 filtrava `distribution_queue` pelo alcance da sessão e o `assign_lead` usava essa mesma fila: com sessão de usuário na conexão (sdr_handoff, varredura) o motor não achava corretor fora do alcance — pego pelo `tests/07_core_fixes.sql`. Agora `distribution_queue_interna` (sem EXECUTE para authenticated) é a fila do motor e a `distribution_queue` da tela só a recorta; teste no `99_visibilidade_diretor_fila.sql`. `validate-schema.sh --all` voltou a rodar (Docker consertado) e passou inteiro depois de pôr `push_subscriptions` na exceção declarada de escrita do `06_anon_surface.sql`. **Disco:** o script removia o container sem `-v` e cada rodada deixava ~50 MB de volume anônimo (273 volumes, 13 GB) — agora `docker rm -f -v`. Removidos a pedido do dono o Supabase local do FACEIMOB (não usado; tudo é remoto) e tudo da FixWear; MVCore e nexoflow intocados | `0148_roleta_independe_da_sessao.sql`, `tests/99_visibilidade_diretor_fila.sql`, `tests/06_anon_surface.sql`, `scripts/validate-schema.sh` |
| 15/09/2026 | **Pedido do cliente de 14/09: Status 1 por grupo, CCA com 18 colunas, 2º envio, mensagem obrigatória e contador.** (1) **Status 1 não é a etapa do funil**: é o grupo do Status 2 (VENDA, PROPOSTA, LEGADO, DISTRATO, OFF). Trocar o Status 2 troca o Status 1; troca manual só com `deals.edit_status_group`. Admin cadastra, ativa e desativa as duas listas (`deals.manage_statuses`); LEGADO fica ativo até o cliente desligar. O rótulo "Etapa (Status 1)" virou "Etapa". **0149 aplicada na homologação** (hash confere): 34 Status 2 no catálogo, backfill OFF 4.529 · VENDA 1.839 · LEGADO 876 · DISTRATO 170 · PROPOSTA 165, 0 sem grupo; etapa, desfecho, `updated_at`, pontos e avisos idênticos antes e depois. Os 4.409 negócios com "OFF" gravado no Status 2 ficam no grupo OFF — recalcular pelo STATUS2 original do Bubble reabriria ~2.900 negócios mortos. (2) **CCA (0150, validada no Docker e revisada, NÃO aplicada)**: as 18 sínteses viram as colunas e cada coluna grava seu Status 2; o caso só anda por `move_cca_case` com mensagem; 2º envio "Análise p/ virar negócio" só com crédito aprovado e passando pelo gerente; envio ao gerente exige mensagem; `cca_send_counts` conta envios por CPF nas duas esteiras (começa do zero); reentrada avisa a CCA; negócio em Aprovado/Contrato não volta para Em análise; 'esteira' e 'aprovado' pontuam uma vez por negócio. **Deploy casado:** a 0150 muda a assinatura do envio e trava o update direto do caso — só entra junto com o front novo. (3) **Defeito corrigido:** aprovar o caso na CCA levava para "Aprovado" negócio já em Contrato, Fechado ou Perdido (926 vendas e 3.036 perdidos expostos) — agora só avança negócio aberto e atrás da etapa (`isBehindStage`). (4) **Front pronto, não publicado:** tela "Status do negócio" no Pipeline (Status 1 e Status 2: nome, grupo, cor, ordem, ativo, criar; travados com cadeado), catálogo lido do banco no lugar da lista fixa, Status 1 no negócio/tabela/filtro/planilha (o formulário não manda o Status 1 quando só o Status 2 muda — o banco deriva); CCA move por `move_cca_case` com mensagem que abre vazia, casos sem coluna saem do quadro com contagem no cabeçalho, selos "Ágil N"/"Virar N", "Status 2 gravado" por coluna; aba Anexos com esteira e mensagem obrigatória. Revisão pegou e corrigiu: negócio aberto com "19. REPROVADO" gravado pela coluna ia para Perdido em qualquer salvamento (`choosesLoss`). typecheck, lint e vitest (122 arquivos, 1.436 testes) verdes; e2e ajustados e NÃO rodados (dependem da 0150 no banco e da service role) | `0149_status_catalogo.sql`, `0150_cca_colunas_e_envio.sql`, `dealStatuses.ts`, `DealStatusSettingsDialog.tsx`, `statuses.ts`, `newSchema.ts`, `ccaData.ts`, `CcaStageSettingsDialog.tsx`, `DealDocumentUpload.tsx`, `documents.ts`, `tests/99_status_catalogo.sql`, `tests/99_cca_colunas_e_envio.sql`, `guards.ts`, `CcaMoveDialog.tsx`, `ScheduleVisitDialog.tsx`, `DealForm.tsx`, `CONTEXT.md` |
| 15/09/2026 | **CCA e Pipeline: período de 30 dias no banco, uma rolagem só e configuração de admin (0151).** O dono abriu a CCA publicada e reprovou: lenta, tudo travando, três rolagens na mesma tela e indicadores de alturas diferentes. Causa: a tela baixava os 7.560 casos e os 7.579 negócios e filtrava no navegador. (1) **Carga por período**: `listLegacyDeals(signal, { createdFrom, createdTo, ids })` filtra `created_at` no banco; a CCA filtra `cca_cases.submitted_at` e busca só os negócios dos casos do período; filtro De/Até com padrão dos últimos 30 dias (fuso de São Paulo, "Até" inclusivo) e ordem do mais recente para o mais antigo nas duas telas. Medido no navegador com dados simulados: 190 de 620 casos no período, negócios em 2 lotes por id, quadro na tela em 414 ms (1,47 s com CPU 4x). **Consequência aceita:** Pipeline e Dashboard não dividem mais a mesma carga — abrir o Dashboard depois do Pipeline baixa a base inteira de novo. Fechar e reabrir mês passaram a contar no banco, para não dependerem do filtro. (2) **Layout da CCA**: página sem rolagem, quadro como única área rolável (horizontal e vertical), cabeçalho de coluna fixo, sem rolagem por coluna, 19 indicadores com a mesma altura e largura. (3) **Configuração só admin/sócio**: `cca_stages_write` e `document_types_write` passam de `has_permission('cca.review')` para `is_admin()`, e os botões somem para o CCA, que continua movendo casos. (4) **Modais**: o `DialogContent` compartilhado não tinha altura máxima nem rolagem — diálogo sem classe própria saía da tela e não rolava; agora todo diálogo cabe na janela com só o corpo rolando, e as listas de Select montam ao abrir (digitar no "Status do negócio" caiu de ~120 ms para ~1 ms por tecla). (5) `cca_send_counts(p_deal_ids uuid[] default null)` devolve só os negócios da tela (134 ms contra 587 ms). Provado em navegador com sessão e dados falsos, a 1366x768 e 375x812, como admin e como gerente somente leitura; e2e e app logado seguem sem rodar | `0151_cca_config_so_admin.sql`, `tests/99_cca_config_so_admin.sql`, `newSchema.ts`, `data.ts`, `filters.ts`, `DealsToolbar.tsx`, `CloseMonthDialog.tsx`, `ReopenMonthDialog.tsx`, `Pipeline.tsx`, `CcaPipeline.tsx`, `ccaData.ts`, `CcaBoard.tsx`, `CcaStageSettingsDialog.tsx`, `DealStatusSettingsDialog.tsx`, `ui/dialog.tsx` |
| 17/09/2026 | **Padrão visual do app e ajustes do Pipeline (0152).** Pedido do dono, com duas referências de tela (painel azul-marinho no escuro, CRM branco com barra azul no claro) e a regra de que tudo que é visual vale para o app inteiro, num ponto compartilhado. (1) **Tema**: cores tiradas dos pixels do próprio logo (azul #215FAA, azul claro #608DC4, menta #A7D5B9, amarelo #E5CC34); escuro sai de quase preto para fundo #101F37 e cartão #192C48; claro ganha barra lateral no azul da marca, com tokens novos (`sidebar-muted-foreground`, `sidebar-highlight`, `sidebar-destructive`) porque rótulo, item ativo e "Sair" sumiam no azul; âmbar segue como destaque. Contraste medido: texto 15,1–16,7 e barra lateral 6,4 (claro) / 13,3 (escuro). (2) **Raio**: `--radius` de 1rem para 0.5rem — cartões de 24 px para 12 px e campos de 20 px para 10 px, no app todo pela escala do Tailwind. (3) **KPIs**: componente `KpiGrid` (flex com quebra, cartões de largura igual e última linha centralizada) no Dashboard (4+3), CCA, Leads, Atividades, Marketing e esqueletos. (4) **Mês**: o padrão passa a ser sempre o mês corrente (`currentMonthBase`), `pickOpenMonth`/`defaultMonthOf` apagadas — o Dashboard abria em 02/2027 por causa de 1 negócio com mês-base errado; o seletor virou a prop `period` do `PageHeader`, centralizada de verdade (o grupo é que estava centralizado, com o selo empurrando o controle). (5) **Marca d'água**: `BrandMotif` passou a desenhar o símbolo real em SVG, com ~75% visível. (6) **Pipeline**: troféu no lugar da medalha que não aparecia, cor por construtora escolhida no cadastro e na edição (`developers.color`, `#RRGGBB` cobrado por CHECK; escrita continua em `developers_write`), ordem construtora → status do catálogo → mais recente (substitui a ordem só por data de 15/09; a CCA continua por data) e corretor 2 no cartão e na tabela. (7) De passagem: a tabela do Pipeline rolava de lado a 375 px por causa do texto de leitor de tela dos cabeçalhos ordenáveis (`relative` no contêiner resolveu), e a leitura de construtoras voltou a `id,name,color` depois da 0152 — com `*` ia e-mail, telefone e observação de todas as construtoras para o navegador de qualquer corretor. **Provado** em build de produção com sessão falsa: 85 screenshots nos dois temas a 1366 e 375 px, seletor com desvio 0 px, cartões com 12 px de raio, ordem e cores conferidas. **Pendente de decisão do dono:** a marca d'água do cabeçalho fica atrás do avatar e lê-se como formas soltas | `0152_cor_da_construtora.sql`, `tests/99_cor_da_construtora.sql`, `index.css`, `tailwind.config.ts`, `shared/KpiGrid.tsx`, `shared/BrandMotif.tsx`, `shared/PageHeader.tsx`, `dashboard/data.ts`, `AdminDevelopers.tsx`, `PodiumCards.tsx`, `DealCard.tsx`, `DealsTable.tsx`, `filters.ts`, `lib/tone.ts`, `newSchema.ts`, `design-system.md` |
| 18/09/2026 | **Sem pílula no app e kanban colorido (0153).** Pedido do cliente: o raio de 17/09 (cartão 8 px, campo 6 px) é o padrão e nada mais fica com o arredondado grande — abas, "Últimos 30 dias", botões, selos, menu lateral e interruptor saem de `rounded-full` para a escala; `rounded-full` só em círculo de verdade (avatar, bolinha, spinner), cobrado em `radius-scale.test.ts`. Kanban no estilo do print do CV CRM, na CCA e no Pipeline (os dois únicos quadros): cabeçalho em seta na cor da coluna com nome, total em R$ e quantidade (`KanbanColumnHeader`), texto preto ou branco pelo maior contraste (`textOn`, mínimo 4,6:1 nas cores atuais), corpo neutro e cartão com borda esquerda na cor da coluna; na CCA a faixa de indicadores ganha a cor no topo e o número fica na cor do texto. Cor da coluna: CCA em `cca_stages.color` (agora `#RRGGBB` ou chave antiga, CHECK na 0153, que também deu uma cor própria a cada coluna semeada, na família do significado), escolhida em "Gerenciar estágios" com o seletor nativo (`shared/ColorField`, o mesmo das construtoras); Pipeline em `pipeline_stages.color`, que já era hex e nunca tinha sido lido. **Provado** em build de produção com sessão falsa nos dois temas a 1366 e 375 px: nenhum raio acima de 8 px fora de círculo, sem rolagem lateral da página, CCA com rolagem única e cabeçalho preso. De passagem: Douglas excluiu em 17/09 as colunas EM ANÁLISE e PENDENTE; os 1.923 casos delas foram, pela regra da tela, para EM PROCESSAMENTO e RETORNO À ESTEIRA ÁGIL — pendente saber se foi de propósito | `0153_cor_das_colunas_cca.sql`, `tests/99_cor_das_colunas_cca.sql`, `KanbanColumnHeader.tsx`, `CcaBoard.tsx`, `DealsKanban.tsx`, `DealCard.tsx`, `CcaStageSettingsDialog.tsx`, `shared/ColorField.tsx`, `lib/tone.ts`, `ui/button.tsx`, `ui/badge.tsx`, `ui/tabs.tsx`, `ui/switch.tsx`, `ui/sidebar.tsx`, `radius-scale.test.ts` |

### Pendências operacionais novas (não são código)

1. **Meta global de vendas**: o Dashboard agora lê `goals` (`scope='global'`,
   `metric='sales'`) e mostra "—" até existir a linha — não há UI para meta
   global; inserir via SQL ou evoluir a tela Equipes.
2. **`META_APP_SECRET`**: cadastrar em Integrações quando o Douglas fornecer,
   para o webhook passar a recusar POST sem assinatura.
3. **Webhook de mensagens no painel da Meta**: apontar o campo *messages* do
   app WhatsApp para `/functions/v1/whatsapp-inbound-webhook` (mesmo verify
   token do meta-ads).
4. **Cron `faceimob-notify-dispatch`** segue pausado até haver credencial do
   WhatsApp (decisão de 05/08 continua valendo).
5. **Template de e-mail no remoto**: colar o conteúdo de
   `supabase/templates/magic_link.html` em Authentication → Emails → Magic Link
   (ou `supabase config push`). Enquanto não for feito, quem tentar entrar
   recebe um e-mail **sem código nenhum** para digitar — a tela pede seis
   dígitos e o template padrão só manda link. É o item mais urgente da lista.
6. **SMTP do Brevo em Authentication → Emails**: o remetente embutido do
   Supabase recusa endereço fora da equipe e tem cota baixa por hora.
